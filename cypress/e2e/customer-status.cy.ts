describe("Customer Status Management", () => {
  it.todo("changes customer status via inline dropdown with reason")
  it.todo("shows destructive confirmation dialog when blacklisting")
  it.todo("requires reason of at least 10 characters")
  it.todo("blacklisted customer is blocked from new loan issuance")
  it.todo("status change is reflected immediately on customer profile")
})
