---
phase: quick
plan: 260322-s7a
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema/loans.ts
  - src/types/index.ts
  - src/services/loan.service.ts
  - src/services/payment.service.ts
  - src/app/(app)/loans/page.tsx
  - src/app/(app)/loans/[loanId]/loan-detail-client.tsx
  - src/app/(app)/customers/[id]/page.tsx
  - src/components/customers/customer-search-bar.tsx
  - src/services/__tests__/loan.service.test.ts
  - src/services/__tests__/payment.service.test.ts
  - src/services/__integration__/loan.service.test.ts
  - src/services/__integration__/payment.service.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Creating a loan sets its status to 'active' immediately"
    - "No code path references 'pending' as a loan status"
    - "Deleting all payments from a loan keeps it 'active' (not reverted to pending)"
    - "The LoanStatus type only contains 'active' and 'fully_paid'"
  artifacts:
    - path: "src/lib/db/schema/loans.ts"
      provides: "pgEnum with only active and fully_paid"
      contains: "loanStatusEnum"
    - path: "src/types/index.ts"
      provides: "LoanStatus type without pending"
  key_links:
    - from: "src/services/loan.service.ts"
      to: "src/lib/db/schema/loans.ts"
      via: "insert with status: active"
    - from: "src/services/payment.service.ts"
      to: "src/lib/db/schema/loans.ts"
      via: "no pending transitions remain"
---

<objective>
Remove the "pending" loan status entirely. Loans are created as "active" immediately since disbursement happens off-app. The only remaining statuses are "active" and "fully_paid".

Purpose: Simplify the loan lifecycle -- the pending->active transition is unnecessary ceremony since the loan officer gives money off-app before recording the loan.
Output: Schema enum, services, UI, and tests updated; no reference to "pending" as a loan status remains.
</objective>

<execution_context>
@/Users/faridmatovu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/faridmatovu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/db/schema/loans.ts
@src/types/index.ts
@src/services/loan.service.ts
@src/services/payment.service.ts
@src/app/(app)/loans/page.tsx
@src/app/(app)/loans/[loanId]/loan-detail-client.tsx
@src/app/(app)/customers/[id]/page.tsx
@src/components/customers/customer-search-bar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update schema, types, and service logic to remove pending status</name>
  <files>
    src/lib/db/schema/loans.ts,
    src/types/index.ts,
    src/services/loan.service.ts,
    src/services/payment.service.ts
  </files>
  <action>
1. **src/lib/db/schema/loans.ts** (line 4-8): Change loanStatusEnum to only `["active", "fully_paid"]`. Change the default on the status column (line 17) from `"pending"` to `"active"`.

2. **src/types/index.ts** (line 20): Change `LoanStatus` from `"pending" | "active" | "fully_paid"` to `"active" | "fully_paid"`.

3. **src/services/loan.service.ts** (line 82): Change `status: "pending"` to `status: "active"` in the loan insert values.

4. **src/services/payment.service.ts**:
   - Line 175-186 (recordPayment status transitions): Remove the `else if (loan.status === "pending")` branch entirely. The only transition that remains is `active -> fully_paid` when `allocation.loanFullyPaid` is true.
   - Line 111: Update the JSDoc comment to remove "pending -> active" mention.
   - Lines 418-423 (deletePayment): When no active payments remain after deletion, set status to `"active"` instead of `"pending"`. The loan stays active (it was disbursed), it just has no recorded payments.

NOTE: This requires a DB migration to alter the enum. Generate a new Drizzle migration:
   - Run `npx drizzle-kit generate` to create the migration SQL.
   - The generated SQL needs to handle the Postgres enum alteration. If drizzle-kit does not handle enum value removal cleanly, manually write the migration SQL: `ALTER TYPE loan_status RENAME TO loan_status_old; CREATE TYPE loan_status AS ENUM ('active', 'fully_paid'); ALTER TABLE loans ALTER COLUMN status TYPE loan_status USING status::text::loan_status; ALTER TABLE loans ALTER COLUMN status SET DEFAULT 'active'; DROP TYPE loan_status_old;`
   - But FIRST: any existing rows with status='pending' must be updated to 'active' before the enum change: `UPDATE loans SET status = 'active' WHERE status = 'pending';`
   - Write the migration file manually if drizzle-kit output is incorrect.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Schema enum has only "active" and "fully_paid". Loan creation inserts as "active". Payment service has no pending references. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Update UI components and search filters to remove pending references</name>
  <files>
    src/app/(app)/loans/page.tsx,
    src/app/(app)/loans/[loanId]/loan-detail-client.tsx,
    src/app/(app)/customers/[id]/page.tsx,
    src/components/customers/customer-search-bar.tsx
  </files>
  <action>
1. **src/app/(app)/loans/page.tsx** (line 26): Remove the `if (status === "pending") return "secondary"` branch from the badge variant helper. Only "active" and "fully_paid" branches remain.

2. **src/app/(app)/loans/[loanId]/loan-detail-client.tsx**:
   - Line 60: Remove `if (status === "pending") return "secondary"` from badge variant helper.
   - Lines 192-196: Remove the entire conditional block that shows the "Loan pending disbursement" amber banner when `loan.status === "pending"`.

3. **src/app/(app)/customers/[id]/page.tsx** (line 60): Remove `if (status === "pending") return "secondary"` from the loan status badge variant helper. (Note: lines 164+ reference `pendingStatus` for *customer* status changes -- leave those alone, they are unrelated to loan status.)

4. **src/components/customers/customer-search-bar.tsx** (line 119): Remove `<SelectItem value="pending">Pending</SelectItem>` from the loan status filter dropdown.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>No UI component references "pending" as a loan status. Badge helpers only handle "active" and "fully_paid". Search filter only shows active/fully_paid options.</done>
</task>

<task type="auto">
  <name>Task 3: Update all tests to remove pending loan status references</name>
  <files>
    src/services/__tests__/loan.service.test.ts,
    src/services/__tests__/payment.service.test.ts,
    src/services/__integration__/loan.service.test.ts,
    src/services/__integration__/payment.service.test.ts
  </files>
  <action>
1. **src/services/__tests__/loan.service.test.ts** (line 43): Change mock loan `status: "pending"` to `status: "active"`. Update any assertions that expect "pending".

2. **src/services/__tests__/payment.service.test.ts**:
   - Line 35: Change mock loan `status: "pending"` to `status: "active"`.
   - Line 197-200: Remove or rewrite the test "recordPayment: transitions loan status pending -> active on first payment" -- this transition no longer exists. Replace with a test confirming that recording a first payment on an active loan keeps it active (no status change unless fully paid).
   - Update any other mock objects that set status to "pending".

3. **src/services/__integration__/loan.service.test.ts**:
   - Line 54: Change `expect(result.status).toBe("pending")` to `expect(result.status).toBe("active")`.
   - Line 180: Change `expect(fetched.status).toBe("pending")` to `expect(fetched.status).toBe("active")`.

4. **src/services/__integration__/payment.service.test.ts**:
   - Line 69-72: Rewrite test "1. first payment activates a pending loan" -- rename to "1. first payment on active loan keeps it active" and update the assertion. The loan starts as "active" now, and first payment should keep it active.
   - Line 359-388: Rewrite test "12. deleting only payment reverts loan to pending" -- rename to "12. deleting only payment keeps loan active" and change `expect(reverted.status).toBe("pending")` to `expect(reverted.status).toBe("active")`.

After updating, run the unit tests to confirm they pass.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All unit and integration tests pass. No test references "pending" as a loan status. Test assertions match the new behavior (loans start as "active").</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- zero type errors
2. `npx vitest run` -- all tests pass
3. `grep -rn '"pending"' src/ --include='*.ts' --include='*.tsx' | grep -v 'pending-approval' | grep -v 'pendingVerifications' | grep -v 'pendingStatus' | grep -v 'is pending' | grep -v 'mutation is pending'` -- should return NO results related to loan status
</verification>

<success_criteria>
- The loanStatusEnum in Postgres only contains "active" and "fully_paid"
- New loans are created with status "active" immediately
- No service, UI, or test code references "pending" as a loan status
- All Vitest tests pass
- TypeScript compiles without errors
- A migration file exists to handle the enum change for existing databases
</success_criteria>

<output>
After completion, create `.planning/quick/260322-s7a-remove-pending-loan-status-loans-are-act/260322-s7a-SUMMARY.md`
</output>
