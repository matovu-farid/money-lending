---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/pglite-server.ts
  - scripts/pglite-global-setup.ts
  - docs/superpowers/plans/2026-03-22-pglite-test-database.md
  - docs/superpowers/specs/2026-03-22-pglite-test-database-design.md
  - src/services/__integration__/notification.service.test.ts
  - .planning/codebase/TESTING.md
  - .planning/codebase/CONCERNS.md
autonomous: true
requirements: [QUICK-REMOVE-PGLITE]

must_haves:
  truths:
    - "No PGLite source files exist in the project"
    - "No PGLite references remain in application or test code"
    - "No PGLite npm packages installed"
    - "Integration tests still pass using local Postgres"
  artifacts:
    - path: "scripts/pglite-server.ts"
      provides: "DELETED"
    - path: "scripts/pglite-global-setup.ts"
      provides: "DELETED"
    - path: "docs/superpowers/plans/2026-03-22-pglite-test-database.md"
      provides: "DELETED"
    - path: "docs/superpowers/specs/2026-03-22-pglite-test-database-design.md"
      provides: "DELETED"
  key_links: []
---

<objective>
Remove all PGLite-related code, scripts, and documentation from the project. The project uses local Postgres for all testing — PGLite was an experiment that is no longer needed.

Purpose: Clean up dead code and unused dependencies so the project only references its actual test database (local Postgres).
Output: No PGLite files, references, or packages remain.
</objective>

<execution_context>
@/Users/faridmatovu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/faridmatovu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
PGLite packages were already removed from package.json. What remains:
- `scripts/pglite-server.ts` — standalone PGLite wire-protocol server (unused)
- `scripts/pglite-global-setup.ts` — vitest globalSetup that starts/stops PGLite (unused, vitest.integration.config.ts does NOT reference it)
- `docs/superpowers/plans/2026-03-22-pglite-test-database.md` — planning doc
- `docs/superpowers/specs/2026-03-22-pglite-test-database-design.md` — spec doc
- A comment in `src/services/__integration__/notification.service.test.ts:57` mentioning PGLite
- Possible references in `.planning/codebase/TESTING.md` and `.planning/codebase/CONCERNS.md`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete PGLite scripts and documentation</name>
  <files>scripts/pglite-server.ts, scripts/pglite-global-setup.ts, docs/superpowers/plans/2026-03-22-pglite-test-database.md, docs/superpowers/specs/2026-03-22-pglite-test-database-design.md</files>
  <action>
    Delete the following files:
    - `scripts/pglite-server.ts`
    - `scripts/pglite-global-setup.ts`
    - `docs/superpowers/plans/2026-03-22-pglite-test-database.md`
    - `docs/superpowers/specs/2026-03-22-pglite-test-database-design.md`

    Then remove PGLite node_modules remnants:
    - Run `pnpm store prune` to clean any cached PGLite packages
    - Check `pnpm-lock.yaml` for any remaining `@electric-sql/pglite` or `@electric-sql/pglite-socket` entries. If they exist and are not depended on by any package.json entry, they are stale lock entries — run `pnpm install` to regenerate the lockfile without them.
  </action>
  <verify>
    <automated>test ! -f scripts/pglite-server.ts && test ! -f scripts/pglite-global-setup.ts && test ! -f docs/superpowers/plans/2026-03-22-pglite-test-database.md && test ! -f docs/superpowers/specs/2026-03-22-pglite-test-database-design.md && echo "PASS: all PGLite files deleted"</automated>
  </verify>
  <done>All four PGLite-related files are deleted. No PGLite packages in node_modules.</done>
</task>

<task type="auto">
  <name>Task 2: Remove PGLite references from remaining source and planning files</name>
  <files>src/services/__integration__/notification.service.test.ts, .planning/codebase/TESTING.md, .planning/codebase/CONCERNS.md</files>
  <action>
    1. In `src/services/__integration__/notification.service.test.ts` around line 57, update the comment:
       - Change "PGlite collapses sequential inserts into the same timestamp" to "Sequential inserts may collapse into the same timestamp" (remove the PGLite-specific mention, the defensive coding is still valid).

    2. Search `.planning/codebase/TESTING.md` and `.planning/codebase/CONCERNS.md` for any PGLite references. Update or remove those lines to reflect that tests use local Postgres only.

    3. Do a final project-wide grep for "pglite" (case-insensitive, excluding node_modules, .git, and pnpm-lock.yaml) to confirm zero references remain. If any are found in other files, clean those too.
  </action>
  <verify>
    <automated>! grep -ri "pglite" --include="*.ts" --include="*.tsx" --include="*.js" --include="*.md" --exclude-dir=node_modules --exclude-dir=.git --exclude="pnpm-lock.yaml" . && echo "PASS: no PGLite references remain"</automated>
  </verify>
  <done>Zero PGLite references in any source, test, or planning file (excluding node_modules and lockfile).</done>
</task>

</tasks>

<verification>
1. `grep -ri "pglite" --exclude-dir=node_modules --exclude-dir=.git --exclude="pnpm-lock.yaml" .` returns no results
2. `ls scripts/pglite-*` returns "No such file or directory"
3. `ls docs/superpowers/*/2026-03-22-pglite*` returns "No such file or directory"
4. Integration tests still pass: `pnpm vitest run --config vitest.integration.config.ts` (requires local Postgres running)
</verification>

<success_criteria>
- All PGLite files deleted (4 files)
- All PGLite references removed from source and docs
- No PGLite npm packages in node_modules
- Project grep for "pglite" returns zero matches (excluding node_modules/lockfile)
</success_criteria>

<output>
After completion, create `.planning/quick/260326-9ss-remove-pglite-from-the-project-completel/260326-9ss-SUMMARY.md`
</output>
