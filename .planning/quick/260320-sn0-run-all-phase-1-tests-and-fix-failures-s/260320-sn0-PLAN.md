---
phase: quick
plan: 260320-sn0
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
must_haves:
  truths:
    - "All Vitest unit tests pass (vitest run exits 0)"
    - "All Cypress E2E tests pass (cypress run exits 0)"
  artifacts: []
  key_links: []
---

<objective>
Run all Phase 1 tests — Vitest unit tests and Cypress E2E tests — identify failures, fix them, and confirm all tests pass.

Purpose: Phase 1 foundation code must have passing tests before moving to Phase 2 or presenting checkpoints.
Output: All test suites green.
</objective>

<execution_context>
@/Users/faridmatovu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/faridmatovu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Vitest unit tests, fix all failures</name>
  <files>
    src/lib/interest/__tests__/engine.test.ts,
    src/services/__tests__/customer.service.test.ts,
    src/services/__tests__/loan.service.test.ts
  </files>
  <action>
    1. Run `pnpm test` (vitest run) and capture output.
    2. For each failing test:
       - Read the test file and the source file it tests.
       - Determine whether the failure is in the test expectation (stale/wrong assertion) or the source code (bug).
       - Fix the appropriate file. Prefer fixing tests if they have stale expectations from refactors; fix source code if the logic is genuinely wrong.
    3. Re-run `pnpm test` to confirm all pass.

    Key constraints from project decisions:
    - All monetary arithmetic uses BigNumber (no native JS floats).
    - Interest is calculated on-demand from loan history.
    - Loan statuses: pending, active, fully_paid only.
    - No Zod — TypeScript types are sufficient.
    - writeAuditLog uses direct await (not Effect.runPromise) inside Drizzle tx.
  </action>
  <verify>
    <automated>cd /Users/faridmatovu/projects/money-lending && pnpm test 2>&1 | tail -30</automated>
  </verify>
  <done>All 3 Vitest test files pass. `pnpm test` exits with code 0.</done>
</task>

<task type="auto">
  <name>Task 2: Run Cypress E2E tests, fix all failures</name>
  <files>
    cypress/e2e/registration.cy.ts,
    cypress/e2e/auth-gate.cy.ts,
    cypress/e2e/customer-crud.cy.ts,
    cypress/e2e/loan-wizard.cy.ts,
    cypress/e2e/loans-list.cy.ts,
    cypress/e2e/admin-panel.cy.ts
  </files>
  <action>
    1. Ensure test database is set up: run `pnpm db:test:setup` if DATABASE_URL_TEST is configured, or check scripts/setup-test-db.ts for what it does.
    2. Start the dev server with CYPRESS=true and DATABASE_URL_TEST, then run Cypress headlessly:
       ```
       CYPRESS=true DATABASE_URL=$DATABASE_URL_TEST pnpm next dev -p 3001 &
       # Wait for server ready
       npx wait-on http://localhost:3001 --timeout 30000
       npx cypress run --config baseUrl=http://localhost:3001
       kill %1
       ```
    3. For each failing test:
       - Read the Cypress spec and the page/component it tests.
       - Check if selectors are stale (page was rebuilt in 01-07), assertions are wrong, or app code has a bug.
       - Fix the spec or source file as appropriate.
       - CYPRESS=true env var bypasses email verification for E2E (per project decisions).
    4. Re-run full Cypress suite to confirm all pass.

    Key constraints:
    - Email verification is enabled but CYPRESS=true bypasses to in-memory store for E2E.
    - First registered user is auto-promoted to Super Admin.
    - Better Auth session API used for all auth middleware.
    - shadcn uses @base-ui/react primitives (not Radix).
  </action>
  <verify>
    <automated>cd /Users/faridmatovu/projects/money-lending && CYPRESS=true npx cypress run --config baseUrl=http://localhost:3001 2>&1 | tail -40</automated>
  </verify>
  <done>All 6 Cypress E2E spec files pass. `cypress run` exits with code 0.</done>
</task>

</tasks>

<verification>
- `pnpm test` exits 0 (all Vitest unit tests pass)
- `cypress run` exits 0 (all Cypress E2E tests pass)
</verification>

<success_criteria>
All Phase 1 tests — 3 Vitest unit test files and 6 Cypress E2E spec files — pass without failures.
</success_criteria>

<output>
After completion, create `.planning/quick/260320-sn0-run-all-phase-1-tests-and-fix-failures-s/260320-sn0-SUMMARY.md`
</output>
