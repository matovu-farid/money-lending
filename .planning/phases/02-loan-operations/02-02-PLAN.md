---
phase: 02-loan-operations
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/(app)/loans/[loanId]/page.tsx
  - src/app/(app)/loans/[loanId]/payments/new/page.tsx
  - src/components/ui/textarea.tsx
  - src/components/ui/form.tsx
autonomous: true
requirements: [LOAN-06, LOAN-07]

must_haves:
  truths:
    - "Loan officer can navigate to a loan detail page and see all payments ordered by date"
    - "Loan officer can record a new payment with amount and date via a form"
    - "Loan officer can edit a payment with a required reason"
    - "Loan officer can delete a payment with a required reason"
    - "Deleted payments appear crossed out in the list"
    - "Outstanding balance is prominently displayed above the payments table"
  artifacts:
    - path: "src/app/(app)/loans/[loanId]/page.tsx"
      provides: "Loan detail page with payments list, edit/delete dialogs"
      min_lines: 100
    - path: "src/app/(app)/loans/[loanId]/payments/new/page.tsx"
      provides: "Payment recording form"
      min_lines: 50
  key_links:
    - from: "src/app/(app)/loans/[loanId]/page.tsx"
      to: "src/actions/payment.actions.ts"
      via: "Server Action calls for edit/delete"
      pattern: "import.*editPaymentAction.*deletePaymentAction.*from.*actions/payment"
    - from: "src/app/(app)/loans/[loanId]/payments/new/page.tsx"
      to: "src/actions/payment.actions.ts"
      via: "recordPaymentAction Server Action"
      pattern: "import.*recordPaymentAction.*from.*actions/payment"
---

<objective>
Build the loan detail page with payments list and the payment recording form -- the primary UI for the daily transaction loop.

Purpose: Loan officers need to view a loan's payment history, record new payments, and edit/delete existing payments with accountability (reason required). This is the most-used screen in the application.

Output: Loan detail page at `/loans/[loanId]` with payments table, edit/delete dialogs, and a record-payment form at `/loans/[loanId]/payments/new`.
</objective>

<execution_context>
@/Users/faridmatovu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/faridmatovu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-loan-operations/02-CONTEXT.md
@.planning/phases/02-loan-operations/02-UI-SPEC.md
@.planning/phases/02-loan-operations/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 02-01 outputs -->

From src/actions/payment.actions.ts:
```typescript
export async function recordPaymentAction(input: RecordPaymentInput): Promise<{ data: Payment } | { error: string }>
export async function editPaymentAction(input: EditPaymentInput): Promise<{ data: Payment } | { error: string }>
export async function deletePaymentAction(input: DeletePaymentInput): Promise<{ data: Payment } | { error: string }>
```

From src/services/payment.service.ts:
```typescript
export const getPaymentsForLoan: (loanId: string) => Effect<Payment[], LoanNotFound | DatabaseError>
```

From src/types/index.ts:
```typescript
export interface RecordPaymentInput {
  loanId: string
  paymentDate: string
  amount: string
  note?: string
}
export interface EditPaymentInput {
  paymentId: string
  amount?: string
  paymentDate?: string
  reason: string
}
export interface DeletePaymentInput {
  paymentId: string
  reason: string
}
export type Payment = InferSelectModel<typeof payments>
// Payment includes: id, loanId, paymentDate, amount, interestPortion, principalPortion,
// principalBalanceBefore, principalBalanceAfter, recordedBy, editReason, deletedAt, deletedBy, deleteReason
```

From src/services/loan.service.ts:
```typescript
export const getLoan: (id: string) => Effect<Loan, LoanNotFound | DatabaseError>
```

shadcn components installed: badge, button, card, dialog, dropdown-menu, input, label, select, separator, sheet, sonner, table, tooltip
shadcn components to install first: textarea, form
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn components and build record-payment form page</name>
  <files>
    src/components/ui/textarea.tsx,
    src/components/ui/form.tsx,
    src/app/(app)/loans/[loanId]/payments/new/page.tsx
  </files>
  <read_first>
    src/app/(app)/loans/new/page.tsx,
    src/app/(app)/customers/new/page.tsx,
    src/actions/payment.actions.ts,
    src/types/index.ts,
    src/app/globals.css,
    .planning/phases/02-loan-operations/02-UI-SPEC.md,
    node_modules/next/dist/docs/01-app/02-guides/forms.md
  </read_first>
  <action>
    **Step 1: Install shadcn components**
    ```bash
    npx shadcn@latest add textarea
    npx shadcn@latest add form
    npx shadcn@latest add alert
    ```

    **Step 2: Create `/loans/[loanId]/payments/new/page.tsx`**

    A client component (`"use client"`) with a single-column vertical form:

    - **Page heading:** `text-2xl font-semibold` -- "Record Payment"
    - **Form fields in vertical stack (single column):**
      1. **Payment Date** -- `<Input type="date" />` with `<Label>Payment Date</Label>`. Default value: today's date in `YYYY-MM-DD` format via `new Date().toISOString().split('T')[0]`
      2. **Amount (UGX)** -- `<Input type="text" inputMode="numeric" />` with `<Label>Amount (UGX)</Label>`. No minimum validation (LOAN-09). Show "UGX" prefix via a `<span>` inside a flex container before the input.
      3. **Note (optional)** -- `<Textarea placeholder="Optional note" />` with `<Label>Note</Label>`. Not required.
    - **Submit button:** `<Button>Record Payment</Button>` -- primary variant. Loading state: `disabled` prop + `<Loader2 className="animate-spin" />` icon replacing text with "Recording..." text.
    - **Back link:** `<Link href={"/loans/${loanId}"} className={cn(buttonVariants({ variant: "outline" }))}>Back to Loan</Link>` above the form.

    **Form submission flow:**
    1. On submit, call `recordPaymentAction({ loanId, paymentDate: dateValue + "T00:00:00.000Z", amount: amountValue })`
    2. If `{ data }` returned: show `toast.success("Payment recorded successfully")` via sonner, then `router.push(\`/loans/\${loanId}\`)`
    3. If `{ error }` returned: show `toast.error(result.error)` -- form stays populated (no data loss on error)

    **Params handling:** `const { loanId } = await params` (Next.js 16 -- params is a Promise)

    However, since this is a client component, use `use(params)` from React or pass loanId as a prop from a server wrapper. The recommended pattern: make the page.tsx a server component that reads params and renders a client component:

    ```tsx
    // page.tsx (server component)
    import { RecordPaymentForm } from "./record-payment-form"
    export default async function RecordPaymentPage({ params }: { params: Promise<{ loanId: string }> }) {
      const { loanId } = await params
      return <RecordPaymentForm loanId={loanId} />
    }
    ```

    Create `src/app/(app)/loans/[loanId]/payments/new/record-payment-form.tsx` as the client component.

    **Validation on submit:**
    - Amount must not be empty and must match `/^\d+(\.\d{1,2})?$/`
    - Date must not be empty
    - If validation fails, set inline error text (`text-destructive text-xs`) below the field
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/textarea.tsx exists (shadcn installed)
    - src/app/(app)/loans/[loanId]/payments/new/page.tsx exists
    - page.tsx contains `await params` (Next.js 16 async params)
    - page.tsx or record-payment-form.tsx contains `recordPaymentAction`
    - record-payment-form.tsx contains `toast.success("Payment recorded successfully")`
    - record-payment-form.tsx contains `toast.error(`
    - record-payment-form.tsx contains `type="date"`
    - record-payment-form.tsx contains `Recording` (loading state text)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Record payment form works: amount and date inputs, UGX prefix, submit calls Server Action, success toast and redirect, error toast with form preserved, loading state on button.</done>
</task>

<task type="auto">
  <name>Task 2: Loan detail page with payments table, edit/delete dialogs</name>
  <files>
    src/app/(app)/loans/[loanId]/page.tsx
  </files>
  <read_first>
    src/app/(app)/loans/page.tsx,
    src/app/(app)/customers/[id]/page.tsx,
    src/services/payment.service.ts,
    src/services/loan.service.ts,
    src/actions/payment.actions.ts,
    src/types/index.ts,
    src/lib/db/schema/payments.ts,
    .planning/phases/02-loan-operations/02-UI-SPEC.md,
    .planning/phases/02-loan-operations/02-CONTEXT.md
  </read_first>
  <action>
    **Create `src/app/(app)/loans/[loanId]/page.tsx`** -- a Server Component that fetches loan + payments data, renders the detail view, and includes client-side interactivity via an extracted client component.

    **Architecture:**
    - `page.tsx` is a Server Component: `const { loanId } = await params`, fetches loan and payments via `Effect.runPromise(getLoan(loanId))` and `Effect.runPromise(getPaymentsForLoan(loanId))`
    - Renders a client component `<LoanDetailClient loan={loan} payments={payments} />` for interactive parts (dialogs, dropdowns)

    **Create `src/app/(app)/loans/[loanId]/loan-detail-client.tsx`** as `"use client"`:

    **Page structure:**
    1. **Back link:** `<Link href="/loans" className={cn(buttonVariants({ variant: "outline" }))}>Back to Loans</Link>`
    2. **Loan header section:**
       - Loan reference: `font-mono text-xs` -- `LOAN-{loan.id.slice(0,8).toUpperCase()}`
       - Status badge: `<Badge variant={loan.status === "active" ? "default" : "secondary"}>{loan.status}</Badge>`
       - Principal amount: formatted UGX
       - Interest rate: `{(parseFloat(loan.interestRate) * 100).toFixed(1)}% / month`
       - Start date
       - Customer name (join from loan.customerId if needed, or pass from server component)
    3. **Outstanding balance focal point** (UI-SPEC requirement):
       - `text-2xl font-semibold` for the balance figure
       - `text-xs text-muted-foreground` for the label "Outstanding Balance"
       - Balance = last active payment's `principalBalanceAfter`, or `loan.principalAmount` if no payments
    4. **"Record Payment" CTA:** `<Link href={"/loans/${loanId}/payments/new"} className={cn(buttonVariants())}>Record Payment</Link>`
    5. **"Print Disbursement Receipt" link:** `<Link href={"/receipts/disbursement/${loanId}"} className={cn(buttonVariants({ variant: "outline" }))}>Print Receipt</Link>`
    6. **Payments table** using shadcn Table:
       - Columns: Date | Amount (UGX) | Interest Paid (UGX) | Principal Paid (UGX) | Balance After (UGX) | Recorded By | Actions
       - Rows ordered by `paymentDate ASC`
       - **Soft-deleted rows:** Apply `opacity-60 line-through` classes to all cell text. Action menu shows only a disabled "Deleted" label.
       - **Active rows:** Action menu via `<DropdownMenu>` with `<DropdownMenuTrigger aria-label="Payment actions">` using a three-dot icon (`MoreHorizontal` from lucide-react). Items: "Edit Payment" and "Delete Payment".
       - **UGX formatting:** Use `Intl.NumberFormat('en-UG', { style: 'decimal' })` or the pattern from Phase 1.
    7. **Empty state** (when no payments):
       - Heading: `text-lg font-semibold` -- "No payments recorded"
       - Body: `text-sm text-muted-foreground` -- "Record the first payment against this loan to start tracking repayments."
       - CTA: `<Link href={"/loans/${loanId}/payments/new"}>Record Payment</Link>`

    **Edit Dialog** (shadcn Dialog):
    - Open when "Edit Payment" clicked from dropdown
    - State: `editingPayment` (holds the payment being edited)
    - Fields:
      - Amount: `<Input type="text" defaultValue={editingPayment.amount} />`
      - Payment Date: `<Input type="date" defaultValue={formatDate(editingPayment.paymentDate)} />`
      - Reason: `<Textarea placeholder="Explain what is being corrected and why" />` -- REQUIRED, submit disabled until filled
    - Buttons:
      - "Save Changes" (primary) -- calls `editPaymentAction({ paymentId, amount, paymentDate, reason })`
      - "Discard Changes" (outline) -- closes dialog
    - Loading state on Save Changes button
    - On success: `toast("Payment updated")`, close dialog (page auto-revalidates via revalidatePath)
    - On error: `toast.error(result.error)`

    **Delete Dialog** (shadcn Dialog):
    - Open when "Delete Payment" clicked from dropdown
    - Heading: "Delete payment?"
    - Body: "This payment will be marked as deleted. The record is not permanently removed. All subsequent balances will be recalculated."
    - Reason field: `<Textarea placeholder="Explain why this payment is being deleted" />` -- REQUIRED
    - Buttons:
      - "Delete Payment" (`variant="destructive"`) -- calls `deletePaymentAction({ paymentId, reason })`
      - "Keep Payment" (outline) -- closes dialog
    - Loading state on Delete Payment button
    - On success: `toast("Payment deleted")`, close dialog
    - On error: `toast.error(result.error)`

    All copy text must match the Copywriting Contract in 02-UI-SPEC.md exactly.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(app)/loans/[loanId]/page.tsx exists and contains `await params`
    - page.tsx or loan-detail-client.tsx contains `editPaymentAction`
    - page.tsx or loan-detail-client.tsx contains `deletePaymentAction`
    - loan-detail-client.tsx contains `opacity-60 line-through` (soft-deleted row styling)
    - loan-detail-client.tsx contains `text-2xl font-semibold` (outstanding balance focal point)
    - loan-detail-client.tsx contains `Outstanding Balance` (label text)
    - loan-detail-client.tsx contains `No payments recorded` (empty state)
    - loan-detail-client.tsx contains `Delete payment?` (delete dialog heading)
    - loan-detail-client.tsx contains `Reason for deletion` or `Explain why this payment is being deleted`
    - loan-detail-client.tsx contains `Reason for edit` or `Explain what is being corrected and why`
    - loan-detail-client.tsx contains `aria-label="Payment actions"` (accessibility)
    - loan-detail-client.tsx contains `toast` (sonner import)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Loan detail page shows all payments with edit/delete capability, soft-deleted rows with strikethrough, outstanding balance focal point, and proper dialog flows with required reason fields.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` compiles without errors
- Loan detail page renders payments table with all required columns
- Edit and delete dialogs require reason before submission
- Soft-deleted rows appear with opacity-60 and line-through
- Outstanding balance displayed prominently with text-2xl
- All copywriting matches UI-SPEC contract
</verification>

<success_criteria>
- Loan officer can navigate to /loans/[loanId] and see all payments
- Loan officer can record a new payment at /loans/[loanId]/payments/new
- Loan officer can edit a payment via dialog with required reason
- Loan officer can delete a payment via dialog with required reason
- Deleted payments appear crossed out
- Outstanding balance is the focal point of the page
</success_criteria>

<output>
After completion, create `.planning/phases/02-loan-operations/02-02-SUMMARY.md`
</output>
