---
phase: 01-foundation
plan: 02
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/lib/interest/engine.ts
  - src/lib/interest/engine.test.ts
  - src/lib/interest/index.ts
autonomous: true
requirements:
  - LOAN-03
  - LOAN-04
  - LOAN-10

must_haves:
  truths:
    - "calculateInterest returns correct reducing-balance interest for any number of elapsed days"
    - "Minimum 30-day interest period is enforced even if repaid earlier"
    - "All arithmetic uses BigNumber — no native float operations on money values"
    - "Results are reproducible and match manual calculation"
  artifacts:
    - path: "src/lib/interest/engine.ts"
      provides: "Pure BigNumber interest calculation functions"
      exports: ["calculateInterest", "calculateDailyRate", "calculateLoanSummary", "formatAmount"]
      min_lines: 40
    - path: "src/lib/interest/engine.test.ts"
      provides: "Unit tests for interest engine covering LOAN-03, LOAN-04, LOAN-10"
      min_lines: 50
  key_links:
    - from: "src/lib/interest/engine.ts"
      to: "bignumber.js"
      via: "import BigNumber"
      pattern: "new BigNumber"
    - from: "src/lib/interest/engine.test.ts"
      to: "src/lib/interest/engine.ts"
      via: "import { calculateInterest }"
      pattern: "calculateInterest"
---

<objective>
Build the Interest Engine using TDD — the pure BigNumber calculation functions that all downstream loan operations depend on.

Purpose: The Interest Engine is the financial core. Correctness here is non-negotiable — wrong interest math cascades through every loan, payment, and report. TDD ensures the math is verified before any consumer code is written.
Output: Tested, correct interest calculation functions that both server-side services and client-side wizard can import.
</objective>

<execution_context>
@/Users/faridmatovu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/faridmatovu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- From 01-RESEARCH.md Pattern 6: Interest Engine -->
The Interest Engine is a set of pure functions with NO side effects, NO database calls.
Formula: interest = outstanding_balance x daily_rate x days_elapsed
Daily rate = monthly_rate / 30
Minimum interest period: max(daysElapsed, minInterestDays)
BigNumber.config({ DECIMAL_PLACES: 10, ROUNDING_MODE: BigNumber.ROUND_HALF_UP })
formatAmount uses .toFixed(2) for display/storage

From 01-CONTEXT.md Interest Calculation Preview (Review Step):
- Daily interest amount (UGX)
- Total interest at 30 days
- Total owed at 30 days (principal + interest)
- Minimum interest period reminder
</interfaces>
</context>

<feature>
  <name>Interest Engine (Pure BigNumber Functions)</name>
  <files>src/lib/interest/engine.ts, src/lib/interest/engine.test.ts, src/lib/interest/index.ts</files>
  <behavior>
    RED phase — write failing tests first:

    Test 1: calculateInterest basic — 500,000 UGX at 10%/month for 30 days = 50,000.00 UGX
      input: ("500000", "0.10", 30, 30)
      expected: BigNumber("50000.00")

    Test 2: calculateInterest pro-rated days — 500,000 UGX at 10%/month for 45 days = 75,000.00 UGX
      input: ("500000", "0.10", 45, 30)
      expected: BigNumber("75000.00")

    Test 3: calculateInterest minimum period — 500,000 UGX at 10%/month for 15 days with 30-day minimum = 50,000.00 UGX (charges 30 days, not 15)
      input: ("500000", "0.10", 15, 30)
      expected: BigNumber("50000.00")

    Test 4: calculateInterest 1-day elapsed, 30-day minimum = 30 days charged
      input: ("1000000", "0.10", 1, 30)
      expected: BigNumber("100000.00")

    Test 5: calculateDailyRate — 10%/month = 0.003333... per day
      input: ("0.10")
      expected: BigNumber("0.10").dividedBy(30)

    Test 6: calculateLoanSummary for Review step — returns daily interest, 30-day total interest, total owed at 30 days
      input: ("500000", "0.10", 30)
      expected: { dailyInterest: "1666.67", totalInterest30Days: "50000.00", totalOwed30Days: "550000.00", minInterestDays: 30 }

    Test 7: formatAmount — BigNumber(50000) returns "50000.00"
      input: BigNumber("50000")
      expected: "50000.00"

    Test 8: No native float in engine — verify engine.ts source contains no `*`, `/`, `+`, `-` operators between numbers (only BigNumber methods)
      approach: Read engine.ts source, regex check for bare arithmetic operators on numeric values

    Test 9: calculateInterest with custom minimum period override — 500,000 UGX at 10%/month for 10 days with 15-day override = charges 15 days
      input: ("500000", "0.10", 10, 15)
      expected: BigNumber("25000.00")

    GREEN phase — implement to pass all tests.

    REFACTOR phase — clean up if needed, ensure all tests still pass.
  </behavior>
  <implementation>
    Create `src/lib/interest/engine.ts` with:
    - `BigNumber.config({ DECIMAL_PLACES: 10, ROUNDING_MODE: BigNumber.ROUND_HALF_UP })` at module top
    - `calculateDailyRate(monthlyRateDecimal: string): BigNumber` — returns monthlyRate / 30
    - `calculateInterest(outstandingBalance: string, monthlyRateDecimal: string, daysElapsed: number, minInterestDays: number = 30): BigNumber` — implements the formula with minimum period enforcement
    - `calculateLoanSummary(principalAmount: string, monthlyRateDecimal: string, minInterestDays: number = 30): { dailyInterest: string; totalInterest30Days: string; totalOwed30Days: string; minInterestDays: number }` — for the loan wizard Review step (CONTEXT.md locked decision)
    - `formatAmount(amount: BigNumber): string` — returns `.toFixed(2)`

    Create `src/lib/interest/index.ts` — barrel export:
    ```typescript
    export { calculateInterest, calculateDailyRate, calculateLoanSummary, formatAmount } from "./engine"
    ```

    CRITICAL: No native JS arithmetic operators (`*`, `/`, `+`, `-`) on monetary values. ALL math via BigNumber methods: `.multipliedBy()`, `.dividedBy()`, `.plus()`, `.minus()`.
  </implementation>
</feature>

<verification>
- `pnpm vitest run src/lib/interest/ --reporter=verbose` — all tests pass
- No native float arithmetic in engine.ts (verify with grep for bare arithmetic on numbers)
- Manual verification: 500,000 x 0.10/30 x 30 = 50,000.00 matches calculateInterest output
</verification>

<success_criteria>
- All 9+ tests pass in engine.test.ts
- calculateInterest("500000", "0.10", 30, 30) returns BigNumber equal to "50000.00"
- calculateInterest("500000", "0.10", 15, 30) returns BigNumber equal to "50000.00" (minimum period enforced)
- calculateLoanSummary returns the four fields needed by the loan wizard Review step
- No native float arithmetic anywhere in engine.ts
- Engine is a pure module with zero side effects — no DB imports, no fetch calls
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
