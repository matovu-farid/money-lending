---
phase: 01-foundation
plan: 07
type: execute
wave: 4
depends_on: ["01-04", "01-05", "01-06"]
files_modified:
  - src/app/(app)/customers/page.tsx
  - src/app/(app)/customers/new/page.tsx
  - src/app/(app)/customers/[id]/page.tsx
  - src/app/(app)/loans/new/page.tsx
  - src/app/(app)/admin/page.tsx
  - src/app/(app)/loans/page.tsx
autonomous: false
requirements:
  - CUST-01
  - CUST-02
  - CUST-03
  - LOAN-01
  - LOAN-02
  - LOAN-04
  - LOAN-05
  - AUTH-03
  - AUTH-04

must_haves:
  truths:
    - "Customer list page shows data table with Name, Contact, Status -- row click navigates to profile"
    - "Customer registration form on /customers/new with Full Name, Contact, Address fields"
    - "Customer profile page shows editable basic info, status badge, active loan summary, Issue New Loan CTA"
    - "Loan wizard has 3 steps: Loan Details, Collateral, Review & Confirm"
    - "Review step shows daily interest, total interest at min period, total owed at min period, minimum interest reminder in UGX"
    - "Admin page shows user management table with inline role dropdown"
    - "Role dropdown enforces hierarchy -- options limited to roles below the acting user"
    - "Admin page shows session-based login activity (createdAt as last active date) for AUTH-04"
  artifacts:
    - path: "src/app/(app)/customers/page.tsx"
      provides: "Customer list data table"
      contains: "Table"
    - path: "src/app/(app)/customers/new/page.tsx"
      provides: "Customer registration form"
      contains: "createCustomerAction"
    - path: "src/app/(app)/customers/[id]/page.tsx"
      provides: "Customer profile with edit, loan summary, Issue New Loan CTA"
      min_lines: 40
    - path: "src/app/(app)/loans/new/page.tsx"
      provides: "3-step loan issuance wizard with interest preview"
      contains: "calculateLoanSummary"
      min_lines: 80
    - path: "src/app/(app)/admin/page.tsx"
      provides: "User management table with role dropdown and session activity"
      contains: "ROLE_LEVELS"
  key_links:
    - from: "src/app/(app)/loans/new/page.tsx"
      to: "src/lib/interest/engine.ts"
      via: "client-side calculateLoanSummary import"
      pattern: "calculateLoanSummary"
    - from: "src/app/(app)/customers/new/page.tsx"
      to: "src/actions/customer.actions.ts"
      via: "direct Server Action call on form submit"
      pattern: "createCustomerAction"
    - from: "src/app/(app)/loans/new/page.tsx"
      to: "src/actions/loan.actions.ts"
      via: "direct Server Action call on wizard confirm"
      pattern: "createLoanAction"
    - from: "src/app/(app)/admin/page.tsx"
      to: "src/actions/user.actions.ts"
      via: "direct Server Action call on role change"
      pattern: "assignRole"
---

<objective>
Build all Phase 1 UI pages -- customer list, customer registration, customer profile, loan issuance wizard with interest preview, and admin user management panel.

Purpose: These pages complete the Phase 1 user experience. A Loan Officer can register a customer, view their profile, issue a loan with collateral, and see the interest calculation preview. An Admin can manage user roles. This is the last plan -- it wires frontend to the Server Actions built in prior plans. Per user decision, all data operations call Server Actions directly (no fetch to API routes).
Output: All Phase 1 interactive pages functional and connected to Server Actions.
</objective>

<execution_context>
@/Users/faridmatovu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/faridmatovu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
From src/lib/auth-client.ts (Plan 03):
  export const authClient, useSession

From src/lib/interest/engine.ts (Plan 02):
  export function calculateLoanSummary(principalAmount: string, monthlyRateDecimal: string, minInterestDays?: number): {
    dailyInterest: string; totalInterestAtMinPeriod: string; totalOwedAtMinPeriod: string; minInterestDays: number
  }

From src/types/index.ts (Plan 01 + Plan 04 + Plan 05):
  export type Customer, Loan, LoanStatus ("pending" | "active" | "fully_paid"), CustomerStatus, UserRole
  export interface CreateCustomerInput { fullName: string; contact: string; address: string }
  export interface UpdateCustomerInput { fullName?: string; contact?: string; address?: string }
  export interface CreateLoanInput { customerId: string; principalAmount: string; ... }
  export interface CollateralInput { nature: string; description?: string }
  export const ROLE_LEVELS = { unassigned: 0, loanOfficer: 1, admin: 2, superAdmin: 3 }

Server Actions available (call directly, no fetch):
  From src/actions/customer.actions.ts (Plan 04):
    listCustomersAction() => { data: Customer[] } | { error: string }
    createCustomerAction(input: CreateCustomerInput) => { data: Customer } | { error: string }
    getCustomerAction(id) => { data: Customer } | { error: string }
    updateCustomerAction(id, input: UpdateCustomerInput) => { data: Customer } | { error: string }

  From src/actions/loan.actions.ts (Plan 05):
    listLoansAction() => { data: Loan[] } | { error: string }
    createLoanAction(input: CreateLoanInput) => { data: Loan } | { error: string }

  From src/actions/user.actions.ts (Plan 03):
    assignRole(input: { userId, role }) => { success, role } | { error: string }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build customer list, registration form, and profile page</name>
  <files>src/app/(app)/customers/page.tsx, src/app/(app)/customers/new/page.tsx, src/app/(app)/customers/[id]/page.tsx</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (Customer Registration and Profile page decisions)
    - src/types/index.ts (CreateCustomerInput, UpdateCustomerInput interfaces for form alignment)
    - src/actions/customer.actions.ts (Server Action signatures and return types)
    - src/actions/loan.actions.ts (listLoansAction for customer profile loan summary)
    - src/components/ui/table.tsx (shadcn table component API)
    - src/components/ui/card.tsx (shadcn card for profile sections)
    - src/components/ui/badge.tsx (shadcn badge for status)
    - src/components/ui/input.tsx (shadcn input for forms)
    - src/components/ui/button.tsx (shadcn button)
    - src/types/index.ts (Customer type shape)
  </read_first>
  <action>
1. Create `src/app/(app)/customers/page.tsx` -- customer list:
   - "use client" directive
   - Fetch customers by calling `listCustomersAction()` directly on mount using `useEffect`
   - Display in a shadcn `Table` with columns: Name, Contact, Status
   - Status column uses a `Badge` component (variant based on status: "active" = default, "blacklisted" = destructive, "inactive" = secondary)
   - Row click navigates to `/customers/[id]` using `useRouter().push()`
   - "Add Customer" button at top right, links to `/customers/new`
   - Empty state: "No customers yet. Register your first customer." with CTA button
   - Loading state while fetching

   CRITICAL: Import `listCustomersAction` from `@/actions/customer.actions` and call it directly -- do NOT use `fetch("/api/customers")`.

2. Create `src/app/(app)/customers/new/page.tsx` -- registration form:
   - "use client" directive
   - Page heading: "Register Customer"
   - Full-page inline form (NOT modal, NOT drawer -- per CONTEXT.md locked decision)
   - Form fields using shadcn Input + Label:
     - Full Name (text input, required)
     - Contact (text input, required -- phone or email)
     - Physical Address (text input, required)
   - Submit button "Register Customer"
   - Client-side validation: check required fields are non-empty before submit
   - On submit: call `createCustomerAction({ fullName, contact, address })` directly
   - On success (result has `data`): redirect to /customers/[newId] (customer profile)
   - On error (result has `error`): display error message below form using sonner toast
   - Cancel button: navigates back to /customers

   CRITICAL: Import `createCustomerAction` from `@/actions/customer.actions` and call it directly -- do NOT use `fetch("/api/customers")`.
   NOTE: No Zod client-side validation. Use simple required-field checks (non-empty strings). TypeScript types ensure the shape is correct at compile time.

3. Create `src/app/(app)/customers/[id]/page.tsx` -- customer profile:
   - "use client" directive
   - Fetch customer by calling `getCustomerAction(id)` directly on mount
   - Sections per CONTEXT.md locked decision:
     a. **Basic info** -- Card showing Full Name, Contact, Address. "Edit" button toggles inline edit mode. Save calls `updateCustomerAction(id, updatedFields)` directly. Cancel reverts.
     b. **Status badge** -- Display only in Phase 1: shows "Active", "Blacklisted", or "Inactive" badge. No change action (deferred to Phase 3).
     c. **Active loan summary** -- Card showing loan amount, outstanding balance (principal for now -- full balance calc is later), status badge. If no active loan: "No active loans" message.
        Fetch loans by calling `listLoansAction()` and filter by customerId.
     d. **"Issue New Loan" CTA** -- Button that navigates to `/loans/new?customerId=[id]`
   - Loading state while fetching
   - 404 handling: show "Customer not found" if action returns error

   CRITICAL: Import from `@/actions/customer.actions` and `@/actions/loan.actions` -- do NOT use `fetch("/api/...")`.

   All monetary values displayed in UGX format: no decimals for display (UGX is a whole-number currency in practice), use Intl.NumberFormat with locale "en-UG" and style "decimal".
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(app)/customers/page.tsx contains `"use client"`, imports `listCustomersAction` from `@/actions/customer.actions`, shadcn `Table` import
    - src/app/(app)/customers/page.tsx does NOT contain `fetch("/api/` -- uses Server Action directly
    - src/app/(app)/customers/page.tsx has "Add Customer" button linking to `/customers/new`
    - src/app/(app)/customers/new/page.tsx contains form with `fullName`, `contact`, `address` fields
    - src/app/(app)/customers/new/page.tsx imports `createCustomerAction` from `@/actions/customer.actions` and calls it on submit
    - src/app/(app)/customers/new/page.tsx does NOT contain `fetch("/api/` -- uses Server Action directly
    - src/app/(app)/customers/new/page.tsx does NOT import Zod -- uses simple required-field checks
    - src/app/(app)/customers/new/page.tsx is NOT a modal or drawer (full-page form)
    - src/app/(app)/customers/[id]/page.tsx imports `getCustomerAction` and `updateCustomerAction` from `@/actions/customer.actions`
    - src/app/(app)/customers/[id]/page.tsx does NOT contain `fetch("/api/` -- uses Server Actions directly
    - src/app/(app)/customers/[id]/page.tsx has edit toggle for basic info section
    - src/app/(app)/customers/[id]/page.tsx shows status Badge (display only, no change action)
    - src/app/(app)/customers/[id]/page.tsx has "Issue New Loan" button linking to `/loans/new?customerId=`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Customer list with data table, registration form on dedicated page, and profile page with edit, status badge, loan summary, and Issue New Loan CTA. All connected to Server Actions directly -- no fetch to API routes. No Zod validation -- uses TypeScript types and simple field checks.</done>
</task>

<task type="auto">
  <name>Task 2: Build loan issuance wizard and admin user management page</name>
  <files>src/app/(app)/loans/new/page.tsx, src/app/(app)/loans/page.tsx, src/app/(app)/admin/page.tsx</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (Loan Issuance wizard structure, Interest Calculation Preview, Admin panel decisions)
    - src/lib/interest/engine.ts (calculateLoanSummary function signature and return type -- uses totalInterestAtMinPeriod, NOT totalInterest30Days)
    - src/types/index.ts (CreateLoanInput and CollateralInput interfaces for form field alignment)
    - src/actions/loan.actions.ts (createLoanAction, listLoansAction signatures)
    - src/actions/user.actions.ts (assignRole signature)
    - src/types/index.ts (ROLE_LEVELS for admin role dropdown, Loan type)
    - src/components/ui/select.tsx (shadcn select for role dropdown)
    - src/components/ui/card.tsx (shadcn card for wizard steps)
  </read_first>
  <action>
1. Create `src/app/(app)/loans/new/page.tsx` -- 3-step loan issuance wizard:
   - "use client" directive
   - State: `step` (1, 2, or 3), form data object
   - Read `customerId` from URL search params if present (pre-fill from customer profile CTA)

   **Step 1 -- Loan Details:**
   - Fields:
     - Customer: If customerId in URL, show customer name (call `getCustomerAction(customerId)` to fetch name). Otherwise, dropdown/search to select customer.
     - Amount (text input for UGX -- stored as string for BigNumber safety)
     - Start Date (date input, defaults to today)
     - Interest Rate (text input, defaults to "10" displayed as "% per month"; stored as "0.10")
   - "Next" button to advance to Step 2
   - Validation: amount > 0, start date valid, interest rate > 0

   **Step 2 -- Collateral:**
   - Fields:
     - Nature: select dropdown with options "Land Title", "Vehicle Log Book", "Other" (user can type custom)
     - Description: optional textarea
   - "Back" button to return to Step 1
   - "Next" button to advance to Step 3
   - Validation: nature is required

   **Step 3 -- Review & Confirm:**
   - Summary card showing all entered values
   - **Interest Calculation Preview** (CONTEXT.md locked decision):
     - Import `calculateLoanSummary` from `@/lib/interest` (client-side import -- pure function, no server deps)
     - Call with entered principal amount and interest rate
     - Display (NOTE: field names are `totalInterestAtMinPeriod` and `totalOwedAtMinPeriod`, NOT `totalInterest30Days`):
       - Daily interest amount: `{dailyInterest} UGX`
       - Total interest at minimum period: `{totalInterestAtMinPeriod} UGX`
       - Total owed at minimum period: `{totalOwedAtMinPeriod} UGX`
       - Info text: "Minimum interest period applies even if repaid early" (with the actual period days shown)
   - All amounts displayed formatted as UGX (Intl.NumberFormat)
   - "Back" button to return to Step 2
   - "Issue Loan" button to submit
   - On submit: call `createLoanAction(input)` directly with body matching `CreateLoanInput` interface
     - Convert display interest rate (e.g. "10") to decimal ("0.10") before sending
     - Include collateral object
   - On success (result has `data`): redirect to loan detail or customer profile with success toast
   - On 422-equivalent (result has `error: "Incomplete loan requirements"`): show which fields are missing
   - On error: show error toast

   CRITICAL: Import `createLoanAction` from `@/actions/loan.actions` -- do NOT use `fetch("/api/loans")`.

   Step navigation: visual step indicator (Step 1 of 3, Step 2 of 3, Step 3 of 3) at the top of the wizard.

2. Create `src/app/(app)/loans/page.tsx` -- loan list:
   - "use client" directive
   - Fetch loans by calling `listLoansAction()` directly on mount
   - Display in shadcn Table with columns: Customer (need to join or display ID for now), Principal Amount (UGX), Interest Rate, Status, Start Date
   - Status column uses Badge (3 values only: "pending" = secondary, "active" = default, "fully_paid" = success)
   - "New Loan" button at top right linking to /loans/new
   - Empty state: "No loans issued yet."

   CRITICAL: Import `listLoansAction` from `@/actions/loan.actions` -- do NOT use `fetch("/api/loans")`.

3. Create `src/app/(app)/admin/page.tsx` -- user management table:
   - "use client" directive
   - Fetch users from Better Auth admin API: `authClient.admin.listUsers()`
   - Display in shadcn Table with columns: Name, Email, Role, Status, Last Active
   - Role column: inline `Select` dropdown per CONTEXT.md locked decision
     - Options based on current user's role level (from ROLE_LEVELS):
       - SuperAdmin sees: unassigned, loanOfficer, admin
       - Admin sees: unassigned, loanOfficer
       - Others: no dropdown (read-only display)
     - On change: call `assignRole({ userId, role: selectedRole })` directly
     - On error (result has `error`): show toast with error message (e.g., "Cannot assign role at or above your own level")
     - On success: update table row with new role
   - **Login activity (AUTH-04):** Display "Last Active" column showing the user's most recent session creation date from Better Auth session data. If `authClient.admin.listUsers()` returns session info (e.g. `lastActive` or `createdAt`), use that. Otherwise show user `createdAt` as "Joined" date. This satisfies AUTH-04 by surfacing session-based login activity in the admin interface.
   - Page restricted to admin+ roles -- if current user role < admin, show "Access denied" message

   CRITICAL: Import `assignRole` from `@/actions/user.actions` -- do NOT use `fetch("/api/users/...")`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(app)/loans/new/page.tsx contains `"use client"` and manages `step` state (1, 2, 3)
    - src/app/(app)/loans/new/page.tsx Step 1 has Amount, Start Date, Interest Rate fields
    - src/app/(app)/loans/new/page.tsx Step 2 has Nature select and Description textarea
    - src/app/(app)/loans/new/page.tsx Step 3 imports `calculateLoanSummary` from `@/lib/interest`
    - src/app/(app)/loans/new/page.tsx Step 3 displays dailyInterest, totalInterestAtMinPeriod, totalOwedAtMinPeriod (NOT totalInterest30Days)
    - src/app/(app)/loans/new/page.tsx Step 3 shows text about minimum interest period
    - src/app/(app)/loans/new/page.tsx imports `createLoanAction` from `@/actions/loan.actions` and calls it directly
    - src/app/(app)/loans/new/page.tsx does NOT contain `fetch("/api/` -- uses Server Action directly
    - src/app/(app)/loans/new/page.tsx does NOT import Zod -- uses TypeScript types for form data
    - src/app/(app)/loans/new/page.tsx reads `customerId` from search params
    - src/app/(app)/loans/page.tsx imports `listLoansAction` from `@/actions/loan.actions` and calls it directly
    - src/app/(app)/loans/page.tsx does NOT contain `fetch("/api/` -- uses Server Action directly
    - src/app/(app)/admin/page.tsx contains `ROLE_LEVELS` import and role Select dropdown
    - src/app/(app)/admin/page.tsx imports `assignRole` from `@/actions/user.actions` and calls it directly
    - src/app/(app)/admin/page.tsx does NOT contain `fetch("/api/` -- uses Server Action directly
    - src/app/(app)/admin/page.tsx restricts access for non-admin users
    - src/app/(app)/admin/page.tsx contains "Last Active" column header or `createdAt` reference for login activity display (AUTH-04)
    - src/app/(app)/admin/page.tsx renders user `createdAt` or session date in the table (AUTH-04 grep target: `createdAt` or `lastActive`)
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Loan wizard with 3 steps including interest preview using correct field names. Loan list page. Admin page with inline role assignment and login activity display. All pages connected to Server Actions directly -- no fetch to API routes. No Zod -- uses TypeScript types. Uses shadcn/ui components.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: Complete Phase 1 user flows</name>
  <files>n/a</files>
  <action>Human verification of all Phase 1 user flows: customer registration, profile editing, loan issuance with interest preview, admin role management, and interest math correctness.</action>
  <verify>
    <automated>pnpm dev --port 3000 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Dev server starts without errors
    - Customer can be registered at /customers/new and appears in list
    - Customer profile shows editable info, status badge, loan summary, Issue New Loan button
    - Loan wizard has 3 steps with interest preview on Step 3
    - Interest preview math is correct: 1,000,000 UGX at 10%/month shows daily=3,333.33, 30-day=100,000.00, total=1,100,000.00
    - Admin page shows user table with role dropdowns and last active date
    - Role hierarchy enforced in dropdown options
  </acceptance_criteria>
  <what-built>Customer registration, profile, loan issuance wizard with interest preview, admin user management with login activity -- all wired to Server Actions directly (no API route fetch calls, no Zod validation)</what-built>
  <how-to-verify>
    Prerequisites: PostgreSQL running, migrations applied (`npx drizzle-kit push` or `npx drizzle-kit migrate`), dev server running (`pnpm dev`).

    **Flow 1 -- Customer Registration:**
    1. Navigate to /customers/new
    2. Fill in Full Name, Contact, Address
    3. Submit -- should redirect to customer profile
    4. Verify profile shows entered info, status badge, "Issue New Loan" button

    **Flow 2 -- Customer Edit:**
    1. On customer profile, click Edit
    2. Change a field, save
    3. Verify change persists on page refresh

    **Flow 3 -- Loan Issuance:**
    1. Click "Issue New Loan" from customer profile (or navigate to /loans/new)
    2. Step 1: Enter amount (e.g., 500000), verify start date defaults to today, interest rate defaults to 10%
    3. Step 2: Select collateral nature, optionally add description
    4. Step 3: Verify interest preview shows:
       - Daily interest amount
       - Total interest at minimum period
       - Total owed at minimum period
       - Minimum interest period reminder text
    5. Click "Issue Loan" -- should succeed
    6. Check /loans page -- new loan appears in list

    **Flow 4 -- Loan Blocking (CUST-04):**
    1. Try creating a loan for a customer with incomplete data
    2. Should show error with missing field names

    **Flow 5 -- Admin Role Management:**
    1. Log in as Super Admin or Admin
    2. Navigate to /admin
    3. See user list with role dropdowns and last active column
    4. Change a user's role -- should update immediately
    5. Verify hierarchy: Admin cannot assign "admin" or "superAdmin"

    **Flow 6 -- Interest Math Spot Check:**
    1. In loan wizard Step 3, enter 1,000,000 UGX at 10%/month
    2. Verify: Daily interest = 3,333.33, interest at min period = 100,000.00, Total owed = 1,100,000.00

    **Flow 7 -- Auth Gate (proxy.ts):**
    1. Open a private/incognito window and navigate to /dashboard
    2. Should redirect to /login
    3. Register a new account -- should land on /pending-approval (unassigned role)
    4. Verify /pending-approval shows clear message and sign-out button
    5. Try navigating to /dashboard -- should redirect back to /pending-approval
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
  <done>Human has verified all Phase 1 user flows: customer CRUD, loan issuance with interest preview, admin role management with login activity, auth gate with role-based redirects, and interest calculation correctness. All operations use Server Actions -- no API route fetch calls, no Zod validation.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `pnpm dev` starts and all pages render without runtime errors
- Customer CRUD works end-to-end through UI via Server Actions
- Loan wizard calculates interest correctly using client-side Interest Engine
- Admin role management respects hierarchy via Server Action
- Admin page shows login activity via createdAt or lastActive (AUTH-04)
- All monetary values displayed in UGX format
- NO `fetch("/api/` calls in any UI component (except Better Auth client which uses its own internal fetch)
- NO Zod imports in any UI component -- form validation uses simple field checks
</verification>

<success_criteria>
- Customer list, registration, and profile pages functional (CUST-01, CUST-02)
- Loan wizard creates loans with collateral via Server Action (LOAN-01, CUST-03)
- Wizard Review step shows correct interest calculations using totalInterestAtMinPeriod/totalOwedAtMinPeriod (LOAN-03, LOAN-04)
- Loan is perpetual with 30-day minimum interest period and 10%/month default rate (LOAN-02)
- Loan status visible in list with 3-status badges: pending, active, fully_paid (LOAN-05 v1)
- Admin page manages user roles with hierarchy via Server Action (AUTH-03, AUTH-05)
- Login activity visible in admin table via session createdAt (AUTH-04)
- All amounts in UGX format (INFR-03)
- All data operations use Server Actions -- no fetch to Route Handlers, no Zod validation
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-07-SUMMARY.md`
</output>
