---
phase: 01-foundation
plan: 06
type: execute
wave: 3
depends_on: ["01-01", "01-03"]
files_modified:
  - src/components/ui/button.tsx
  - src/components/ui/input.tsx
  - src/components/ui/label.tsx
  - src/components/ui/card.tsx
  - src/components/ui/table.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/dropdown-menu.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/select.tsx
  - src/components/ui/separator.tsx
  - src/components/ui/sheet.tsx
  - src/components/ui/sonner.tsx
  - src/components/ui/tooltip.tsx
  - src/components/layout/app-shell.tsx
  - src/components/layout/sidebar.tsx
  - src/components/layout/top-bar.tsx
  - src/app/(app)/layout.tsx
  - src/app/(auth)/layout.tsx
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/register/page.tsx
  - src/app/(auth)/forgot-password/page.tsx
  - src/app/pending-approval/page.tsx
  - src/app/(app)/dashboard/page.tsx
  - src/app/globals.css
autonomous: false
requirements:
  - INFR-03
  - AUTH-01

must_haves:
  truths:
    - "App shell has top bar with branding and collapsible left sidebar"
    - "Sidebar shows all nav groups from day one — unbuilt sections are grayed out but visible"
    - "User avatar and logout live at the bottom of the sidebar"
    - "Auth pages (login, register, forgot-password) use custom components — no Better Auth UI"
    - "Unassigned users land on /pending-approval with clear message and no sidebar"
    - "Login uses email + password only — no username field"
    - "All pages are responsive for desktop and tablet"
  artifacts:
    - path: "src/components/layout/app-shell.tsx"
      provides: "App shell wrapping sidebar + top bar + content area"
      min_lines: 20
    - path: "src/components/layout/sidebar.tsx"
      provides: "Collapsible sidebar with all nav groups and grayed-out future sections"
      contains: "Dashboard"
      min_lines: 50
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Custom login page with email+password form"
      contains: "signIn"
    - path: "src/app/pending-approval/page.tsx"
      provides: "Pending approval landing for unassigned users"
      min_lines: 10
    - path: "src/app/(app)/layout.tsx"
      provides: "Protected route group layout with app shell"
      contains: "AppShell"
  key_links:
    - from: "src/app/(auth)/login/page.tsx"
      to: "src/lib/auth-client.ts"
      via: "signIn function"
      pattern: "signIn"
    - from: "src/components/layout/sidebar.tsx"
      to: "src/lib/auth-client.ts"
      via: "signOut function"
      pattern: "signOut"
    - from: "src/app/(app)/layout.tsx"
      to: "src/components/layout/app-shell.tsx"
      via: "wraps children in app shell"
      pattern: "AppShell"
---

<objective>
Install shadcn/ui components, build the app shell (sidebar + top bar), create auth pages (login, register, forgot-password), pending-approval page, and the protected route group layout.

Purpose: This is the visual skeleton of the entire application. Every authenticated page lives inside the app shell. Auth pages are the entry point for every user. The sidebar nav structure from day one (per user decision) means staff see the full system scope even before all features are built.
Output: Working app shell, all auth pages, pending-approval page, and route group layouts.
</objective>

<execution_context>
@/Users/faridmatovu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/faridmatovu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
From src/lib/auth-client.ts (Plan 03):
  export const authClient — full client
  export const { signIn, signUp, signOut, useSession } — destructured helpers

From 01-CONTEXT.md locked decisions:
  Sidebar groups: (ungrouped) Dashboard; Operations: Customers, Loans, Payments; Capital: Creditors, Expenses & Income; Insights: Reports; System: Admin
  Unbuilt sections grayed out, not hidden
  User avatar and logout at bottom of sidebar
  Auth pages: Custom, no Better Auth UI components
  Login: Email + password only
  Pending approval: No sidebar, clear message + contact info
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn/ui components and build app shell layout</name>
  <files>src/components/ui/*, src/components/layout/app-shell.tsx, src/components/layout/sidebar.tsx, src/components/layout/top-bar.tsx, src/app/(app)/layout.tsx, src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (current Tailwind v4 setup — add shadcn CSS variables)
    - src/app/layout.tsx (root layout to understand font/html structure)
    - .planning/phases/01-foundation/01-CONTEXT.md (locked sidebar structure decisions)
    - .planning/phases/01-foundation/01-RESEARCH.md (shadcn/ui Tailwind v4 pitfall)
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/layout.md (layout convention)
  </read_first>
  <action>
1. Install shadcn/ui components via CLI (these are copy-pasted into the codebase, no runtime dep):
   ```bash
   pnpm dlx shadcn@latest add button input label card table badge dropdown-menu dialog select separator sheet sonner tooltip
   ```
   This generates files in `src/components/ui/`. Also installs `lucide-react` for icons if not already present.

2. Update `src/app/globals.css` to include shadcn CSS variables if the `shadcn init` step didn't already do it. Verify the file contains CSS variables like `--background`, `--foreground`, `--primary`, etc., and that the Tailwind v4 `@import "tailwindcss"` is present.

3. Create `src/components/layout/top-bar.tsx`:
   - Fixed top bar with: left side = app branding text "Lending Manager", right side = empty for now (user info moves to sidebar bottom per CONTEXT.md)
   - Height: `h-14`
   - Border bottom
   - Responsive: `px-4 md:px-6`

4. Create `src/components/layout/sidebar.tsx`:
   - Collapsible sidebar that toggles between full width (~240px) and icon-only rail (~60px)
   - Use `useState` for collapsed state, toggle button at top
   - Navigation groups per CONTEXT.md locked decision:
     - *Ungrouped:* Dashboard (icon: LayoutDashboard, href: /dashboard)
     - *Operations:* Customers (Users icon, /customers), Loans (Banknote icon, /loans), Payments (CreditCard icon, grayed out — Phase 2)
     - *Capital:* Creditors (Landmark icon, grayed), Expenses & Income (Receipt icon, grayed)
     - *Insights:* Reports (BarChart3 icon, grayed)
     - *System:* Admin (Shield icon, /admin)
   - Grayed-out items: `opacity-50 pointer-events-none` with a tooltip "Coming soon"
   - Bottom section: User avatar (circle with initials from session), user name/email, logout button
   - Use `useSession()` from auth-client for user info
   - Use `signOut()` from auth-client for logout
   - Use `usePathname()` from `next/navigation` for active state highlighting
   - Icons from `lucide-react`

5. Create `src/components/layout/app-shell.tsx`:
   - Combines TopBar + Sidebar + main content area
   - Layout: sidebar on left, main content on right, top bar spans full width above both
   - Main content area: `flex-1 overflow-auto p-4 md:p-6`
   - Responsive: On smaller screens, sidebar can be a Sheet (slide-out) triggered by a hamburger in the top bar

6. Create `src/app/(app)/layout.tsx`:
   ```typescript
   import { AppShell } from "@/components/layout/app-shell"

   export default function AppLayout({ children }: { children: React.ReactNode }) {
     return <AppShell>{children}</AppShell>
   }
   ```

7. Create `src/app/(app)/dashboard/page.tsx` — minimal placeholder:
   ```typescript
   export default function DashboardPage() {
     return (
       <div>
         <h1 className="text-2xl font-semibold">Dashboard</h1>
         <p className="text-muted-foreground mt-2">Welcome to Lending Manager. Dashboard metrics will appear here in Phase 3.</p>
       </div>
     )
   }
   ```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/button.tsx exists (shadcn component installed)
    - src/components/ui/table.tsx exists (shadcn component installed)
    - src/components/layout/sidebar.tsx contains "Dashboard", "Customers", "Loans", "Payments", "Creditors", "Reports", "Admin"
    - src/components/layout/sidebar.tsx contains "opacity-50" or "pointer-events-none" for grayed-out items
    - src/components/layout/sidebar.tsx contains `signOut` import from auth-client
    - src/components/layout/sidebar.tsx contains `useSession` import from auth-client
    - src/components/layout/top-bar.tsx contains "Lending Manager" branding text
    - src/components/layout/app-shell.tsx imports both TopBar and Sidebar components
    - src/app/(app)/layout.tsx imports and renders AppShell
    - src/app/(app)/dashboard/page.tsx exists with heading text
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>shadcn/ui components installed. App shell with collapsible sidebar, top bar, and content area complete. All nav groups visible with future sections grayed out. Dashboard placeholder in place.</done>
</task>

<task type="auto">
  <name>Task 2: Create auth pages (login, register, forgot-password) and pending-approval page</name>
  <files>src/app/(auth)/layout.tsx, src/app/(auth)/login/page.tsx, src/app/(auth)/register/page.tsx, src/app/(auth)/forgot-password/page.tsx, src/app/pending-approval/page.tsx</files>
  <read_first>
    - src/lib/auth-client.ts (signIn, signUp for form submissions)
    - src/components/ui/button.tsx (shadcn button component API)
    - src/components/ui/input.tsx (shadcn input component API)
    - src/components/ui/card.tsx (shadcn card component API)
    - src/components/ui/label.tsx (shadcn label component API)
    - .planning/phases/01-foundation/01-CONTEXT.md (auth UX decisions)
  </read_first>
  <action>
1. Create `src/app/(auth)/layout.tsx` — centered card layout, no sidebar:
   ```typescript
   export default function AuthLayout({ children }: { children: React.ReactNode }) {
     return (
       <div className="flex min-h-screen items-center justify-center bg-muted/40 p-4">
         <div className="w-full max-w-md">{children}</div>
       </div>
     )
   }
   ```

2. Create `src/app/(auth)/login/page.tsx`:
   - "use client" directive (uses signIn from auth-client)
   - Card with header "Sign in to Lending Manager"
   - Form fields: Email (type="email"), Password (type="password")
   - Submit button "Sign in" — calls `signIn.email({ email, password })` from auth-client
   - Loading state on submit button
   - Error display below form (from signIn error response)
   - Link to /register ("Don't have an account? Register")
   - Link to /forgot-password ("Forgot your password?")
   - NO username field (email + password only per CONTEXT.md)

3. Create `src/app/(auth)/register/page.tsx`:
   - "use client" directive
   - Card with header "Create your account"
   - Form fields: Name, Email (type="email"), Password (type="password"), Confirm Password
   - Submit button "Create account" — calls `signUp.email({ name, email, password })` from auth-client
   - Client-side validation: passwords must match, email format, name non-empty
   - Error display below form
   - Link to /login ("Already have an account? Sign in")
   - After successful signup, redirect to /pending-approval (since defaultRole is "unassigned"). NOTE: For the very first user, the databaseHook in Plan 01-03 auto-promotes them to superAdmin. The redirect to /pending-approval still happens client-side, but proxy.ts will immediately redirect them to /dashboard since their role is superAdmin (not "unassigned"). No special client-side logic needed here.

4. Create `src/app/(auth)/forgot-password/page.tsx`:
   - "use client" directive
   - Card with header "Reset your password"
   - Form field: Email
   - Submit button "Send reset link" — calls Better Auth's password reset flow via authClient
   - Success message: "If an account exists with that email, you'll receive a reset link"
   - Link to /login ("Back to sign in")

5. Create `src/app/pending-approval/page.tsx`:
   - No sidebar, no app shell (sits outside (app) route group)
   - Centered card layout (similar to auth layout but distinct)
   - Clear heading: "Account Pending Approval"
   - Body text: "Your account has been created successfully. An administrator will assign your role shortly. You'll be able to access the system once your role is confirmed."
   - Contact info line: "Questions? Contact your system administrator."
   - "Sign out" button at bottom — calls `signOut()` from auth-client, redirects to /login
   - This page per CONTEXT.md: "should communicate clearly that the account was created successfully and someone will assign their role"

All pages use shadcn/ui components (Card, Button, Input, Label). All pages are responsive (max-w-md centered, padding adjusts on mobile). NO Better Auth pre-built UI components.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(auth)/layout.tsx renders centered layout with `max-w-md`
    - src/app/(auth)/login/page.tsx contains `"use client"` and imports `signIn` from `@/lib/auth-client`
    - src/app/(auth)/login/page.tsx has email and password input fields (type="email" and type="password")
    - src/app/(auth)/login/page.tsx does NOT contain a username field
    - src/app/(auth)/login/page.tsx has link to `/register` and `/forgot-password`
    - src/app/(auth)/register/page.tsx contains `signUp` import from auth-client
    - src/app/(auth)/register/page.tsx has Name, Email, Password, Confirm Password fields
    - src/app/(auth)/forgot-password/page.tsx has email field and reset link button
    - src/app/pending-approval/page.tsx contains "Account Pending Approval" or similar heading
    - src/app/pending-approval/page.tsx contains `signOut` import from auth-client
    - src/app/pending-approval/page.tsx does NOT import AppShell or Sidebar
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>All auth pages built with custom components + shadcn/ui. Login uses email+password only. Pending-approval page communicates clearly. No Better Auth UI components used.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: App shell, auth pages, and navigation</name>
  <files>n/a</files>
  <action>Human verification of the app shell, auth pages, sidebar navigation, and pending-approval page built in Tasks 1 and 2.</action>
  <verify>
    <automated>pnpm dev --port 3000 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Dev server starts without errors
    - Login page renders at /login with email and password fields
    - Register page renders at /register with name, email, password, confirm password fields
    - Pending approval page renders at /pending-approval with clear message
    - First registered user skips /pending-approval and lands on /dashboard (superAdmin role)
    - App shell sidebar shows all nav groups with grayed-out future sections
    - Sidebar collapses to icon rail
    - Responsive layout adapts to tablet width
  </acceptance_criteria>
  <what-built>Complete app shell with collapsible sidebar, top bar, auth pages (login, register, forgot-password), pending-approval page, and dashboard placeholder</what-built>
  <how-to-verify>
    1. Run `pnpm dev` and open http://localhost:3000
    2. You should be redirected to /login (proxy.ts auth gate)
    3. Verify login page: email + password fields, "Sign in" button, links to register and forgot-password
    4. Click "Register" link — verify register page has Name, Email, Password, Confirm Password fields
    5. Click "Forgot password" link — verify page has email field
    6. Register the FIRST user on a fresh database — you should land on /dashboard (NOT /pending-approval) because the first user is auto-promoted to superAdmin by the databaseHook in Plan 01-03
    7. Sign out. Register a SECOND user — this user should land on /pending-approval with "Account Pending Approval" message and sign-out button
    8. Sign back in as the first user (superAdmin) and verify:
       - App shell: top bar with "Lending Manager", collapsible sidebar
       - Sidebar shows all groups: Dashboard, Operations (Customers, Loans, Payments), Capital (Creditors, Expenses & Income), Insights (Reports), System (Admin)
       - Payments, Creditors, Expenses & Income, Reports are grayed out but visible
       - User info and logout at sidebar bottom
       - Sidebar collapses to icon-only rail
       - Dashboard page shows placeholder
    9. Resize browser to tablet width — verify responsive layout
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
  <done>Human has verified the app shell layout, auth pages, sidebar navigation, pending-approval page, and first-user-superadmin flow render correctly and match the CONTEXT.md locked decisions.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `pnpm dev` starts without errors
- Auth pages render correctly and use custom components
- App shell has collapsible sidebar with all nav groups
- Grayed-out sections visible but non-interactive
- First registered user lands on /dashboard (superAdmin), not /pending-approval
- Responsive layout works on desktop and tablet widths
</verification>

<success_criteria>
- App shell with top bar + collapsible sidebar matching CONTEXT.md locked decisions
- All nav groups visible from day one, future sections grayed out
- Custom auth pages (no Better Auth UI) with email+password login
- Pending-approval page with clear message for unassigned users
- First registered user skips pending-approval (auto-promoted to superAdmin by Plan 01-03)
- Responsive for desktop and tablet (INFR-03)
- User can sign in and sign out
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-06-SUMMARY.md`
</output>
