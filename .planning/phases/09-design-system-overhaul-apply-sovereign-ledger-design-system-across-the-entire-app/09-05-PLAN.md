---
phase: 09-design-system-overhaul
plan: 05
type: execute
wave: 5
depends_on: ["09-04"]
files_modified:
  - src/app/(app)/creditors/page.tsx
  - src/app/(app)/creditors/[id]/CreditorProfileClient.tsx
  - src/app/(app)/creditors/[id]/AddInvestmentDialog.tsx
  - src/app/(app)/creditors/[id]/RecordRepaymentDialog.tsx
  - src/app/(app)/expenses/ExpenseListClient.tsx
  - src/app/(app)/income/IncomeListClient.tsx
  - src/app/(app)/transactions/TransactionLogClient.tsx
  - src/app/(app)/admin/page.tsx
  - src/app/(app)/receipts/disbursement/[loanId]/page.tsx
  - src/app/(app)/receipts/repayment/[paymentId]/page.tsx
  - src/app/(app)/reports/portfolio/PortfolioClient.tsx
  - src/app/(app)/reports/pnl/PnlClient.tsx
  - src/app/(app)/reports/balance-sheet/BalanceSheetClient.tsx
  - src/app/(app)/reports/page.tsx
  - cypress/e2e/design-system.cy.ts
autonomous: true
requirements:
  - DS-11
  - DS-12

must_haves:
  truths:
    - "All remaining pages have Sovereign Ledger heading + subtitle"
    - "All numeric values across every page in the app use font-mono tabular-nums"
    - "Receipt pages render correctly with print token resets"
    - "All design-system.cy.ts tests are un-skipped and passing"
  artifacts:
    - path: "cypress/e2e/design-system.cy.ts"
      provides: "Fully enabled design system verification"
  key_links:
    - from: "src/app/(app)/receipts/disbursement/[loanId]/page.tsx"
      to: "src/app/globals.css"
      via: "@media print token resets"
      pattern: "receipt-body"
---

<objective>
Apply Sovereign Ledger typography to all remaining pages (Creditors, Expenses, Income, Transactions, Admin, Reports, Receipts) and enable all design-system.cy.ts tests.

Purpose: Complete the design system rollout across the entire application. Every page should feel like part of the same Sovereign Ledger aesthetic.
Output: 14 updated page files + fully-enabled design-system test suite
</objective>

<execution_context>
@/Users/faridmatovu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/faridmatovu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN.md
@.planning/phases/09-design-system-overhaul-apply-sovereign-ledger-design-system-across-the-entire-app/09-UI-SPEC.md
@.planning/phases/09-design-system-overhaul-apply-sovereign-ledger-design-system-across-the-entire-app/09-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creditors, Expenses, Income, Transactions, Admin typography</name>
  <files>
    src/app/(app)/creditors/page.tsx
    src/app/(app)/creditors/[id]/CreditorProfileClient.tsx
    src/app/(app)/creditors/[id]/AddInvestmentDialog.tsx
    src/app/(app)/creditors/[id]/RecordRepaymentDialog.tsx
    src/app/(app)/expenses/ExpenseListClient.tsx
    src/app/(app)/income/IncomeListClient.tsx
    src/app/(app)/transactions/TransactionLogClient.tsx
    src/app/(app)/admin/page.tsx
  </files>
  <read_first>
    - src/app/(app)/creditors/page.tsx (creditor list page)
    - src/app/(app)/creditors/[id]/CreditorProfileClient.tsx (creditor detail)
    - src/app/(app)/creditors/[id]/AddInvestmentDialog.tsx (investment dialog)
    - src/app/(app)/creditors/[id]/RecordRepaymentDialog.tsx (repayment dialog)
    - src/app/(app)/expenses/ExpenseListClient.tsx (expense list)
    - src/app/(app)/income/IncomeListClient.tsx (income list)
    - src/app/(app)/transactions/TransactionLogClient.tsx (transaction log)
    - src/app/(app)/admin/page.tsx (admin panel)
    - .planning/phases/09-design-system-overhaul-apply-sovereign-ledger-design-system-across-the-entire-app/09-UI-SPEC.md (Typography, Copywriting Contract)
  </read_first>
  <action>
Apply identical patterns from Plan 04 to each of these 8 files:

**Pattern A — Page heading + subtitle:**

| Page | Heading | Subtitle |
|------|---------|----------|
| creditors/page.tsx | "Creditors" | "Capital sources and obligations" |
| creditors/[id]/CreditorProfileClient.tsx | (dynamic name — keep) | "Creditor profile" |
| expenses/ExpenseListClient.tsx | "Expenses" | "Business expenditure tracking" |
| income/IncomeListClient.tsx | "Income" | "Revenue and other income" |
| transactions/TransactionLogClient.tsx | "Transactions" | "Complete transaction history" |
| admin/page.tsx | "Admin" | "System administration" |

Heading pattern (same as Plan 04):
```tsx
<h1 className="text-2xl font-semibold tracking-tight">{title}</h1>
<p className="text-xs font-semibold uppercase tracking-wider text-muted-foreground mt-1">{subtitle}</p>
```

Dialog components (AddInvestmentDialog, RecordRepaymentDialog) do NOT get page headings.

**Pattern B — Numeric values get font-mono:**

For each file, find all elements displaying currency amounts, percentages, counts, timestamps, and IDs. Add `font-mono tabular-nums` to their className.

Specific items per file:
- **creditors/page.tsx**: Total invested amount, outstanding balance per creditor card
- **CreditorProfileClient.tsx**: Investment amounts, repayment amounts, balance, interest rates, dates
- **AddInvestmentDialog.tsx**: Amount field display (not the input itself), success confirmation amount
- **RecordRepaymentDialog.tsx**: Amount field display (not the input itself), success confirmation amount
- **ExpenseListClient.tsx**: Expense amounts in table cells, totals
- **IncomeListClient.tsx**: Income amounts in table cells, totals
- **TransactionLogClient.tsx**: Transaction amounts, dates, IDs
- **admin/page.tsx**: Any numeric system stats, user counts

**Pattern C — Amount table columns right-aligned:**

Same as Plan 04: amount cells get `text-right font-mono tabular-nums`, amount headers get `text-right`.

Do NOT apply font-mono to: names, descriptions, categories, status labels, action buttons.
  </action>
  <verify>
    <automated>npx cypress run --spec cypress/e2e/creditors.cy.ts --spec cypress/e2e/expenses.cy.ts --spec cypress/e2e/income.cy.ts --spec cypress/e2e/transactions.cy.ts --spec cypress/e2e/admin-panel.cy.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - creditors/page.tsx contains `tracking-tight` and subtitle "Capital sources and obligations"
    - ExpenseListClient.tsx contains `tracking-tight` and subtitle "Business expenditure tracking"
    - IncomeListClient.tsx contains `tracking-tight` and subtitle "Revenue and other income"
    - TransactionLogClient.tsx contains `tracking-tight` and subtitle "Complete transaction history"
    - admin/page.tsx contains `tracking-tight` and subtitle "System administration"
    - All 8 files contain `font-mono` applied to numeric value elements
    - No file applies `font-mono` to name or description elements
    - Cypress tests pass for creditors, expenses, income, transactions, admin
  </acceptance_criteria>
  <done>All secondary pages have Sovereign Ledger heading + subtitle. All numeric values use font-mono tabular-nums. Amount columns are right-aligned.</done>
</task>

<task type="auto">
  <name>Task 2: Reports + Receipts typography and full design-system test pass</name>
  <files>
    src/app/(app)/reports/page.tsx
    src/app/(app)/reports/portfolio/PortfolioClient.tsx
    src/app/(app)/reports/pnl/PnlClient.tsx
    src/app/(app)/reports/balance-sheet/BalanceSheetClient.tsx
    src/app/(app)/receipts/disbursement/[loanId]/page.tsx
    src/app/(app)/receipts/repayment/[paymentId]/page.tsx
    cypress/e2e/design-system.cy.ts
  </files>
  <read_first>
    - src/app/(app)/reports/page.tsx (reports index)
    - src/app/(app)/reports/portfolio/PortfolioClient.tsx (portfolio report)
    - src/app/(app)/reports/pnl/PnlClient.tsx (P&L report)
    - src/app/(app)/reports/balance-sheet/BalanceSheetClient.tsx (balance sheet)
    - src/app/(app)/receipts/disbursement/[loanId]/page.tsx (disbursement receipt)
    - src/app/(app)/receipts/repayment/[paymentId]/page.tsx (repayment receipt)
    - cypress/e2e/design-system.cy.ts (current state — check which tests are still skipped)
    - src/app/globals.css (verify @media print block has token resets from Plan 01)
    - .planning/phases/09-design-system-overhaul-apply-sovereign-ledger-design-system-across-the-entire-app/09-UI-SPEC.md (Print Layout Protection, Typography sections)
  </read_first>
  <action>
**Reports pages:**

| Page | Heading | Subtitle |
|------|---------|----------|
| reports/page.tsx | "Reports" | "Financial reporting and analytics" |
| portfolio/PortfolioClient.tsx | "Portfolio Report" | "Loan portfolio analysis" |
| pnl/PnlClient.tsx | "Profit & Loss" | "Revenue and expense summary" |
| balance-sheet/BalanceSheetClient.tsx | "Balance Sheet" | "Assets, liabilities, and equity" |

Apply Pattern A (heading + subtitle) and Pattern B (font-mono on all numeric values) to each report page. Report pages are heavily numeric — ensure ALL currency amounts, percentages, totals, and subtotals use `font-mono tabular-nums`.

**Receipt pages:**

For receipt pages, apply font-mono to all amount values in the receipt body. These pages use a `receipt-body` class for print layout.

Do NOT change the print-specific classes or structure. Only add `font-mono tabular-nums` to elements displaying amounts and `tracking-tight` to the receipt title heading.

Verify that the `@media print` block in globals.css (from Plan 01) has the token resets:
```css
@media print {
  :root {
    --background: oklch(1 0 0);
    --foreground: oklch(0 0 0);
  }
}
```
If this is missing, add it. If present, no action needed.

**Enable all skipped design-system.cy.ts tests:**

Open `cypress/e2e/design-system.cy.ts` and change ALL remaining `it.skip(` to `it(`. Remove all "enable after Plan XX" comments. All tests should now be active:
- Token value tests (enabled since Plan 01)
- Card ring removal test (enable now — Plan 02 completed)
- Sidebar border test (enable now — Plan 03 completed)
- Top-bar border test (enable now — Plan 03 completed)
- Typography/tracking-tight test on dashboard heading (enable now — Plan 04 completed)

Run the full design system test suite to verify everything passes.
  </action>
  <verify>
    <automated>npx cypress run --spec cypress/e2e/design-system.cy.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - reports/page.tsx contains `tracking-tight` and subtitle "Financial reporting and analytics"
    - PortfolioClient.tsx contains `tracking-tight` and `font-mono`
    - PnlClient.tsx contains `tracking-tight` and `font-mono`
    - BalanceSheetClient.tsx contains `tracking-tight` and `font-mono`
    - receipts/disbursement/[loanId]/page.tsx contains `font-mono` on amount values
    - receipts/repayment/[paymentId]/page.tsx contains `font-mono` on amount values
    - cypress/e2e/design-system.cy.ts does NOT contain `it.skip(`
    - All design-system.cy.ts tests pass
    - globals.css @media print block contains `--background: oklch(1 0 0)`
  </acceptance_criteria>
  <done>All report pages have Sovereign Ledger typography. Receipt pages have font-mono on amounts. All design-system.cy.ts tests are enabled and passing. Full design system rollout is complete.</done>
</task>

<task type="auto">
  <name>Task 3: Full Cypress E2E suite regression pass</name>
  <files>cypress/e2e/design-system.cy.ts</files>
  <read_first>
    - cypress/e2e/design-system.cy.ts (verify no skipped tests remain)
  </read_first>
  <action>
Run the FULL Cypress test suite (`npx cypress run`) to verify no regressions across the entire application from the design system changes.

If any tests fail:
1. Read the failing test file
2. Determine if the failure is due to a design system change (e.g., a test asserting a specific class name that was changed)
3. Update the test to match the new design system classes
4. Re-run until all tests pass

Common test fixes needed:
- Tests asserting `rounded-xl` on cards → change to `rounded-lg`
- Tests asserting `ring-1` on cards → remove assertion
- Tests asserting `border-b` on top-bar → remove assertion
- Tests asserting `shadow-lg` on sheets → remove assertion
- Tests asserting specific OKLCH color values → update to new token values
- Tests asserting `font-medium` on table headers → change to `font-semibold`

Do NOT change application code in this task — only fix test assertions.
  </action>
  <verify>
    <automated>npx cypress run 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `npx cypress run` exits with code 0 (all tests pass)
    - No test file contains assertions for `rounded-xl` on card elements
    - No test file contains assertions for `ring-1 ring-foreground/10` on card elements
    - cypress/e2e/design-system.cy.ts has zero `it.skip` calls
  </acceptance_criteria>
  <done>Full Cypress suite passes with zero failures. All test assertions match the Sovereign Ledger design system. Phase 9 is complete.</done>
</task>

</tasks>

<verification>
npx cypress run
</verification>

<success_criteria>
- Every page in the application has Sovereign Ledger heading + subtitle
- Every numeric value in the application uses font-mono tabular-nums
- Receipt pages print correctly (white background, black text)
- All design-system.cy.ts tests enabled and passing
- Full Cypress suite passes with zero failures
- No regressions from design system changes
</success_criteria>

<output>
After completion, create `.planning/phases/09-design-system-overhaul-apply-sovereign-ledger-design-system-across-the-entire-app/09-05-SUMMARY.md`
</output>
