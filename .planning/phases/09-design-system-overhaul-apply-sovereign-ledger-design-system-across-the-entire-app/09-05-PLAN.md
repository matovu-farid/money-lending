---
phase: 09-design-system-overhaul
plan: 05
type: execute
wave: 5
depends_on: ["09-04"]
files_modified:
  - src/app/(app)/creditors/page.tsx
  - src/app/(app)/creditors/[id]/CreditorProfileClient.tsx
  - src/app/(app)/creditors/[id]/AddInvestmentDialog.tsx
  - src/app/(app)/creditors/[id]/RecordRepaymentDialog.tsx
  - src/app/(app)/expenses/ExpenseListClient.tsx
  - src/app/(app)/income/IncomeListClient.tsx
  - src/app/(app)/transactions/TransactionLogClient.tsx
  - src/app/(app)/admin/page.tsx
autonomous: true
requirements:
  - DS-11

must_haves:
  truths:
    - "All secondary pages have Sovereign Ledger heading + subtitle"
    - "All numeric values on creditors, expenses, income, transactions, admin use font-mono tabular-nums"
  artifacts:
    - path: "src/app/(app)/creditors/page.tsx"
      provides: "Creditors page with Sovereign Ledger typography"
      contains: "tracking-tight"
    - path: "src/app/(app)/expenses/ExpenseListClient.tsx"
      provides: "Expenses page with Sovereign Ledger typography"
      contains: "tracking-tight"
  key_links:
    - from: "src/app/(app)/creditors/[id]/CreditorProfileClient.tsx"
      to: "src/components/ui/card.tsx"
      via: "Card component (now rounded-lg, no ring)"
      pattern: "Card"
---

<objective>
Apply Sovereign Ledger typography to secondary pages: Creditors, Expenses, Income, Transactions, and Admin.

Purpose: Complete the typography rollout for non-core pages so every page has consistent heading + subtitle and font-mono on numeric values.
Output: 8 updated page/component files with Sovereign Ledger typography
</objective>

<execution_context>
@/Users/faridmatovu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/faridmatovu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN.md
@.planning/phases/09-design-system-overhaul-apply-sovereign-ledger-design-system-across-the-entire-app/09-UI-SPEC.md
@.planning/phases/09-design-system-overhaul-apply-sovereign-ledger-design-system-across-the-entire-app/09-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creditors pages typography</name>
  <files>
    src/app/(app)/creditors/page.tsx
    src/app/(app)/creditors/[id]/CreditorProfileClient.tsx
    src/app/(app)/creditors/[id]/AddInvestmentDialog.tsx
    src/app/(app)/creditors/[id]/RecordRepaymentDialog.tsx
  </files>
  <read_first>
    - src/app/(app)/creditors/page.tsx (creditor list page)
    - src/app/(app)/creditors/[id]/CreditorProfileClient.tsx (creditor detail)
    - src/app/(app)/creditors/[id]/AddInvestmentDialog.tsx (investment dialog)
    - src/app/(app)/creditors/[id]/RecordRepaymentDialog.tsx (repayment dialog)
    - .planning/phases/09-design-system-overhaul-apply-sovereign-ledger-design-system-across-the-entire-app/09-UI-SPEC.md (Typography, Copywriting Contract)
  </read_first>
  <action>
**Pattern A — Page heading + subtitle:**

| Page | Heading | Subtitle |
|------|---------|----------|
| creditors/page.tsx | "Creditors" | "Capital sources and obligations" |
| creditors/[id]/CreditorProfileClient.tsx | (dynamic name — keep) | "Creditor profile" |

Heading pattern (same as Plan 04):
```tsx
<h1 className="text-2xl font-semibold tracking-tight">{title}</h1>
<p className="text-xs font-semibold uppercase tracking-wider text-muted-foreground mt-1">{subtitle}</p>
```

Dialog components (AddInvestmentDialog, RecordRepaymentDialog) do NOT get page headings.

**Pattern B — Numeric values get font-mono:**

For each file, find all elements displaying currency amounts, percentages, counts, timestamps, and IDs. Add `font-mono tabular-nums` to their className.

Specific items per file:
- **creditors/page.tsx**: Total invested amount, outstanding balance per creditor card
- **CreditorProfileClient.tsx**: Investment amounts, repayment amounts, balance, interest rates, dates
- **AddInvestmentDialog.tsx**: Amount field display (not the input itself), success confirmation amount
- **RecordRepaymentDialog.tsx**: Amount field display (not the input itself), success confirmation amount

**Pattern C — Amount table columns right-aligned:**
Same as Plan 04: amount cells get `text-right font-mono tabular-nums`, amount headers get `text-right`.

Do NOT apply font-mono to: names, descriptions, categories, status labels, action buttons.
  </action>
  <verify>
    <automated>npx cypress run --spec cypress/e2e/creditors.cy.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - creditors/page.tsx contains `tracking-tight` and subtitle "Capital sources and obligations"
    - CreditorProfileClient.tsx contains `tracking-tight`
    - All 4 files contain `font-mono` applied to numeric value elements
    - No file applies `font-mono` to name or description elements
    - Creditors Cypress test passes
  </acceptance_criteria>
  <done>Creditor pages have Sovereign Ledger heading + subtitle. All numeric values use font-mono tabular-nums.</done>
</task>

<task type="auto">
  <name>Task 2: Expenses, Income, Transactions, Admin typography</name>
  <files>
    src/app/(app)/expenses/ExpenseListClient.tsx
    src/app/(app)/income/IncomeListClient.tsx
    src/app/(app)/transactions/TransactionLogClient.tsx
    src/app/(app)/admin/page.tsx
  </files>
  <read_first>
    - src/app/(app)/expenses/ExpenseListClient.tsx (expense list)
    - src/app/(app)/income/IncomeListClient.tsx (income list)
    - src/app/(app)/transactions/TransactionLogClient.tsx (transaction log)
    - src/app/(app)/admin/page.tsx (admin panel)
    - .planning/phases/09-design-system-overhaul-apply-sovereign-ledger-design-system-across-the-entire-app/09-UI-SPEC.md (Typography, Copywriting Contract)
  </read_first>
  <action>
**Pattern A — Page heading + subtitle:**

| Page | Heading | Subtitle |
|------|---------|----------|
| expenses/ExpenseListClient.tsx | "Expenses" | "Business expenditure tracking" |
| income/IncomeListClient.tsx | "Income" | "Revenue and other income" |
| transactions/TransactionLogClient.tsx | "Transactions" | "Complete transaction history" |
| admin/page.tsx | "Admin" | "System administration" |

Heading pattern:
```tsx
<h1 className="text-2xl font-semibold tracking-tight">{title}</h1>
<p className="text-xs font-semibold uppercase tracking-wider text-muted-foreground mt-1">{subtitle}</p>
```

**Pattern B — Numeric values get font-mono:**

- **ExpenseListClient.tsx**: Expense amounts in table cells, totals
- **IncomeListClient.tsx**: Income amounts in table cells, totals
- **TransactionLogClient.tsx**: Transaction amounts, dates, IDs
- **admin/page.tsx**: Any numeric system stats, user counts

**Pattern C — Amount table columns right-aligned:**
Same as Plan 04: amount cells get `text-right font-mono tabular-nums`, amount headers get `text-right`.

Do NOT apply font-mono to: names, descriptions, categories, status labels, action buttons.
  </action>
  <verify>
    <automated>npx cypress run --spec cypress/e2e/expenses.cy.ts --spec cypress/e2e/income.cy.ts --spec cypress/e2e/transactions.cy.ts --spec cypress/e2e/admin-panel.cy.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - ExpenseListClient.tsx contains `tracking-tight` and subtitle "Business expenditure tracking"
    - IncomeListClient.tsx contains `tracking-tight` and subtitle "Revenue and other income"
    - TransactionLogClient.tsx contains `tracking-tight` and subtitle "Complete transaction history"
    - admin/page.tsx contains `tracking-tight` and subtitle "System administration"
    - All 4 files contain `font-mono` applied to numeric value elements
    - No file applies `font-mono` to name or description elements
    - Cypress tests pass for expenses, income, transactions, admin
  </acceptance_criteria>
  <done>All secondary pages have Sovereign Ledger heading + subtitle. All numeric values use font-mono tabular-nums. Amount columns are right-aligned.</done>
</task>

</tasks>

<verification>
npx cypress run --spec cypress/e2e/creditors.cy.ts
npx cypress run --spec cypress/e2e/expenses.cy.ts
npx cypress run --spec cypress/e2e/income.cy.ts
npx cypress run --spec cypress/e2e/transactions.cy.ts
npx cypress run --spec cypress/e2e/admin-panel.cy.ts
</verification>

<success_criteria>
- All secondary pages (Creditors, Expenses, Income, Transactions, Admin) have Sovereign Ledger heading + subtitle
- All numeric values use font-mono tabular-nums
- Amount columns in tables are right-aligned
- No font-mono on non-numeric text
- All relevant Cypress tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-design-system-overhaul-apply-sovereign-ledger-design-system-across-the-entire-app/09-05-SUMMARY.md`
</output>
