---
phase: 09-design-system-overhaul
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/input.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/table.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/sheet.tsx
autonomous: true
requirements:
  - DS-04
  - DS-05
  - DS-06

must_haves:
  truths:
    - "Card component has no ring-1 ring-foreground/10 — tonal depth only"
    - "Card uses rounded-lg (not rounded-xl)"
    - "Button has a tertiary variant with Electric Blue text"
    - "Button base uses rounded-sm (not rounded-lg)"
    - "Badge uses rounded-sm (not rounded-4xl)"
    - "Table rows have border-b border-border/15 (ghost border at 15% opacity)"
    - "Dialog overlay uses backdrop-blur-[24px] and content uses bg-white/85"
    - "Sheet overlay uses backdrop-blur-[24px]"
    - "Input focus uses Electric Blue ring color (inherited from --ring token)"
  artifacts:
    - path: "src/components/ui/button.tsx"
      provides: "Tertiary variant, rounded-sm base"
      contains: "tertiary"
    - path: "src/components/ui/card.tsx"
      provides: "No ring, rounded-lg"
      contains: "rounded-lg"
    - path: "src/components/ui/table.tsx"
      provides: "Ghost border rows"
      contains: "border-border/15"
  key_links:
    - from: "src/components/ui/card.tsx"
      to: "src/app/globals.css"
      via: "bg-card token"
      pattern: "bg-card"
    - from: "src/components/ui/button.tsx"
      to: "src/app/globals.css"
      via: "--ring token for focus"
      pattern: "focus-visible:ring-ring"
---

<objective>
Restyle the 7 primitive UI components to match the Sovereign Ledger spec: sharp corners, no rings on cards, ghost borders on tables, glassmorphism on dialogs/sheets, tertiary button variant.

Purpose: These primitives are used across every page. Updating them here cascades the design system to all consumers automatically.
Output: 7 restyled component files
</objective>

<execution_context>
@/Users/faridmatovu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/faridmatovu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN.md
@.planning/phases/09-design-system-overhaul-apply-sovereign-ledger-design-system-across-the-entire-app/09-UI-SPEC.md
@.planning/phases/09-design-system-overhaul-apply-sovereign-ledger-design-system-across-the-entire-app/09-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs from Plan 01. -->
From src/app/globals.css (after Plan 01):
  --radius: 0.5rem (so rounded-lg = 0.5rem, rounded-sm = 0.3rem via calc)
  --ring: oklch(0.35 0.2 264) (Electric Blue — focus rings automatically use this)
  --border: oklch(0.8 0 0) (ghost border base color)
  --primary: oklch(0 0 0) (true black)
  --primary-foreground: oklch(0.9 0 0) (light gray text on black bg)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Card, Button, Badge, and Input primitives</name>
  <files>
    src/components/ui/card.tsx
    src/components/ui/button.tsx
    src/components/ui/badge.tsx
    src/components/ui/input.tsx
  </files>
  <read_first>
    - src/components/ui/card.tsx (current: has ring-1 ring-foreground/10, rounded-xl)
    - src/components/ui/button.tsx (current: has rounded-lg, no tertiary variant)
    - src/components/ui/badge.tsx (current: has rounded-4xl)
    - src/components/ui/input.tsx (current: has rounded-lg)
    - DESIGN.md (sections 5 and 6 for component rules)
    - .planning/phases/09-design-system-overhaul-apply-sovereign-ledger-design-system-across-the-entire-app/09-UI-SPEC.md (Component Contracts section)
  </read_first>
  <action>
**card.tsx — Card component:**
In the `Card` function, change the className string:
- Replace `rounded-xl` with `rounded-lg`
- Remove `ring-1 ring-foreground/10` entirely (delete this substring)
- Keep everything else identical (gap-4, overflow-hidden, bg-card, py-4, text-sm, text-card-foreground, all data-* and has-* selectors)

The updated className should be:
```
"group/card flex flex-col gap-4 overflow-hidden rounded-lg bg-card py-4 text-sm text-card-foreground has-data-[slot=card-footer]:pb-0 has-[>img:first-child]:pt-0 data-[size=sm]:gap-3 data-[size=sm]:py-3 data-[size=sm]:has-data-[slot=card-footer]:pb-0 *:[img:first-child]:rounded-t-lg *:[img:last-child]:rounded-b-lg"
```
Note: Also update `*:[img:first-child]:rounded-t-xl` to `rounded-t-lg` and `*:[img:last-child]:rounded-b-xl` to `rounded-b-lg`.

In `CardHeader`, change `rounded-t-xl` to `rounded-t-lg`.
In `CardFooter`, change `rounded-b-xl` to `rounded-b-lg`.

**button.tsx — Button component:**
1. In the base cva string (first argument), replace `rounded-lg` with `rounded-sm`.
2. Add a new variant `tertiary` after `link` in the `variant` object:
   ```
   tertiary: "text-[oklch(0.35_0.2_264)] bg-transparent hover:bg-accent",
   ```
3. Update the `Button` function's type to include `"tertiary"` in the variant union (this happens automatically via cva VariantProps, so no manual type change needed).

**badge.tsx — Badge component:**
In the base cva string, replace `rounded-4xl` with `rounded-sm`.

**input.tsx — Input component:**
In the className string, replace `rounded-lg` with `rounded-md`. The focus ring color is already driven by `--ring` token (Electric Blue from Plan 01), so no focus-visible changes needed.
  </action>
  <verify>
    <automated>npx cypress run --spec cypress/e2e/dashboard.cy.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - card.tsx Card className contains `rounded-lg` and does NOT contain `ring-1 ring-foreground/10`
    - card.tsx Card className does NOT contain `rounded-xl`
    - card.tsx CardHeader className contains `rounded-t-lg` not `rounded-t-xl`
    - card.tsx CardFooter className contains `rounded-b-lg` not `rounded-b-xl`
    - button.tsx cva base string contains `rounded-sm` not `rounded-lg`
    - button.tsx variant object contains key `tertiary` with value including `text-[oklch(0.35_0.2_264)]`
    - badge.tsx cva base string contains `rounded-sm` not `rounded-4xl`
    - input.tsx className contains `rounded-md` not `rounded-lg`
    - Dashboard Cypress test passes
  </acceptance_criteria>
  <done>Card has no ring and uses rounded-lg. Button has tertiary variant and uses rounded-sm. Badge uses rounded-sm. Input uses rounded-md. Dashboard renders correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle Table, Dialog, and Sheet primitives</name>
  <files>
    src/components/ui/table.tsx
    src/components/ui/dialog.tsx
    src/components/ui/sheet.tsx
  </files>
  <read_first>
    - src/components/ui/table.tsx (current: border-b on TableRow, no opacity modifier)
    - src/components/ui/dialog.tsx (current: bg-black/10 on overlay, no backdrop-blur, rounded-xl on popup, ring-1 ring-foreground/10 on popup)
    - src/components/ui/sheet.tsx (current: bg-black/10 on overlay, backdrop-blur-xs only, shadow-lg, border-t/border-b/border-l/border-r on sides)
    - DESIGN.md (sections 4 and 2 — elevation/depth and Glass & Ghost rule)
    - .planning/phases/09-design-system-overhaul-apply-sovereign-ledger-design-system-across-the-entire-app/09-UI-SPEC.md (Table, Dialog/Sheet, Component Contracts sections)
  </read_first>
  <action>
**table.tsx — TableRow:**
In `TableRow` className, change `border-b` to `border-b border-border/15`. The full updated className:
```
"border-b border-border/15 transition-colors hover:bg-muted/50 data-[state=selected]:bg-muted"
```

In `TableHead` className, add `text-xs font-semibold uppercase tracking-wider text-muted-foreground` to match the label typography spec. Remove `font-medium` and `text-foreground`. The full updated className:
```
"h-10 px-2 text-left align-middle text-xs font-semibold uppercase tracking-wider text-muted-foreground whitespace-nowrap [&:has([role=checkbox])]:pr-0"
```

In `TableHeader`, change `[&_tr]:border-b` to `[&_tr]:border-b [&_tr]:border-border/15` so the header row also uses ghost border.

**dialog.tsx — DialogOverlay:**
In `DialogOverlay` className, replace `backdrop-blur-xs` with `backdrop-blur-[24px]` (glassmorphism). The full updated className:
```
"fixed inset-0 isolate z-50 bg-black/10 duration-100 supports-backdrop-filter:backdrop-blur-[24px] data-open:animate-in data-open:fade-in-0 data-closed:animate-out data-closed:fade-out-0"
```

**dialog.tsx — DialogContent (Popup):**
In `DialogContent` className:
- Replace `rounded-xl` with `rounded-lg`
- Replace `bg-background` with `bg-white/85` (glassmorphism content)
- Remove `ring-1 ring-foreground/10`

The full updated className:
```
"fixed top-1/2 left-1/2 z-50 grid w-full max-w-[calc(100%-2rem)] -translate-x-1/2 -translate-y-1/2 gap-4 rounded-lg bg-white/85 p-4 text-sm duration-100 outline-none sm:max-w-sm data-open:animate-in data-open:fade-in-0 data-open:zoom-in-95 data-closed:animate-out data-closed:fade-out-0 data-closed:zoom-out-95"
```

**dialog.tsx — DialogFooter:**
In `DialogFooter` className, replace `rounded-b-xl` with `rounded-b-lg`.

**dialog.tsx — DialogContent close button:**
Add `aria-label="Close"` to the close `Button` element (icon-only button a11y).

**sheet.tsx — SheetOverlay:**
In `SheetOverlay` className, replace `backdrop-blur-xs` with `backdrop-blur-[24px]`. The full updated className:
```
"fixed inset-0 z-50 bg-black/10 transition-opacity duration-150 data-ending-style:opacity-0 data-starting-style:opacity-0 supports-backdrop-filter:backdrop-blur-[24px]"
```

**sheet.tsx — SheetContent (Popup):**
In `SheetContent` className:
- Replace `bg-background` with `bg-white/85`
- Remove `shadow-lg` (no drop shadows in Sovereign Ledger)
- Remove all `border-t`, `border-b`, `border-l`, `border-r` instances from the data-[side=*] selectors (no-line rule). Specifically:
  - Remove `data-[side=bottom]:border-t`
  - Remove `data-[side=left]:border-r`
  - Remove `data-[side=right]:border-l`  (note: this is border-l not border-r since it's right-aligned)
  - Remove `data-[side=top]:border-b`

**sheet.tsx — SheetContent close button:**
Add `aria-label="Close"` to the close `Button` element (icon-only button a11y).
  </action>
  <verify>
    <automated>npx cypress run --spec cypress/e2e/dashboard.cy.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - table.tsx TableRow className contains `border-border/15`
    - table.tsx TableHead className contains `text-xs font-semibold uppercase tracking-wider text-muted-foreground`
    - table.tsx TableHead className does NOT contain `font-medium` or `text-foreground` (without muted- prefix)
    - table.tsx TableHeader className contains `border-border/15`
    - dialog.tsx DialogOverlay className contains `backdrop-blur-[24px]` not `backdrop-blur-xs`
    - dialog.tsx DialogContent className contains `bg-white/85` not `bg-background`
    - dialog.tsx DialogContent className contains `rounded-lg` not `rounded-xl`
    - dialog.tsx DialogContent className does NOT contain `ring-1 ring-foreground/10`
    - dialog.tsx DialogFooter className contains `rounded-b-lg` not `rounded-b-xl`
    - dialog.tsx close button has `aria-label="Close"`
    - sheet.tsx SheetOverlay className contains `backdrop-blur-[24px]` not `backdrop-blur-xs`
    - sheet.tsx SheetContent className contains `bg-white/85` not `bg-background`
    - sheet.tsx SheetContent className does NOT contain `shadow-lg`
    - sheet.tsx SheetContent className does NOT contain `border-t` or `border-l` or `border-r` or `border-b` as standalone classes in data-[side=*] selectors
    - sheet.tsx close button has `aria-label="Close"`
    - Dashboard Cypress test passes
  </acceptance_criteria>
  <done>Table rows use 15% ghost borders. Table headers use label typography (uppercase, tracking-wider, semibold). Dialog/Sheet overlays use 24px glassmorphism blur. Dialog/Sheet content uses bg-white/85. Sheet has no shadow or side borders. Close buttons have aria-labels.</done>
</task>

</tasks>

<verification>
npx cypress run --spec cypress/e2e/design-system.cy.ts
npx cypress run --spec cypress/e2e/dashboard.cy.ts
npx cypress run --spec cypress/e2e/loans-list.cy.ts
</verification>

<success_criteria>
- All 7 primitive components match Sovereign Ledger spec
- Card has no ring, uses rounded-lg
- Button has tertiary variant, uses rounded-sm
- Badge uses rounded-sm
- Table rows use ghost borders (border-border/15)
- Table headers use label typography
- Dialog/Sheet use glassmorphism overlay
- Icon-only close buttons have aria-label
- Existing Cypress tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-design-system-overhaul-apply-sovereign-ledger-design-system-across-the-entire-app/09-02-SUMMARY.md`
</output>
