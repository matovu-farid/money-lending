---
phase: 09-design-system-overhaul
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - src/components/layout/sidebar.tsx
  - src/components/layout/top-bar.tsx
  - src/components/layout/app-shell.tsx
autonomous: true
requirements:
  - DS-07
  - DS-08

must_haves:
  truths:
    - "Sidebar has no border-r — separation is purely tonal (bg-sidebar vs bg-background)"
    - "Sidebar collapse toggle area has no border-b"
    - "Sidebar user section has no border-t"
    - "Top bar has no border-b — separation is tonal"
    - "Sidebar group labels use label typography (uppercase, tracking-wider, text-xs, font-semibold)"
    - "Main content area uses bg-background for the base surface tier"
  artifacts:
    - path: "src/components/layout/sidebar.tsx"
      provides: "Border-free sidebar with tonal separation"
    - path: "src/components/layout/top-bar.tsx"
      provides: "Border-free top bar"
    - path: "src/components/layout/app-shell.tsx"
      provides: "Surface-tier aware content area"
  key_links:
    - from: "src/components/layout/sidebar.tsx"
      to: "src/app/globals.css"
      via: "--sidebar token (oklch 0.96)"
      pattern: "bg-sidebar"
    - from: "src/components/layout/app-shell.tsx"
      to: "src/app/globals.css"
      via: "--background token (oklch 0.98)"
      pattern: "bg-background"
---

<objective>
Remove all border separators from layout components (sidebar, top-bar, app-shell) and rely on tonal surface differences for visual separation per the Sovereign Ledger "No-Line Rule."

Purpose: Layout chrome should fade into the background. The sidebar at oklch(0.96) naturally separates from the content area at oklch(0.98) without needing a border.
Output: 3 updated layout component files
</objective>

<execution_context>
@/Users/faridmatovu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/faridmatovu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN.md
@.planning/phases/09-design-system-overhaul-apply-sovereign-ledger-design-system-across-the-entire-app/09-UI-SPEC.md
@.planning/phases/09-design-system-overhaul-apply-sovereign-ledger-design-system-across-the-entire-app/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove borders from Sidebar and TopBar</name>
  <files>
    src/components/layout/sidebar.tsx
    src/components/layout/top-bar.tsx
  </files>
  <read_first>
    - src/components/layout/sidebar.tsx (current: border-r border-sidebar-border on aside, border-b on collapse area, border-t on user section, Separator component)
    - src/components/layout/top-bar.tsx (current: border-b on header)
    - .planning/phases/09-design-system-overhaul-apply-sovereign-ledger-design-system-across-the-entire-app/09-UI-SPEC.md (Sidebar and Surface Tier sections)
  </read_first>
  <action>
**sidebar.tsx changes:**

1. On the `<aside>` element (line ~108), remove `border-r border-sidebar-border` from the className. The updated className:
   ```
   cn(
     "flex flex-col h-full bg-sidebar transition-all duration-200",
     collapsed ? "w-[60px]" : "w-[240px]"
   )
   ```

2. On the collapse toggle container `<div>` (line ~113), remove `border-b border-sidebar-border` from `className="flex items-center justify-end px-2 py-2 border-b border-sidebar-border"`. Updated:
   ```
   "flex items-center justify-end px-2 py-2"
   ```

3. On the user section container `<div>` (line ~213), remove `border-t border-sidebar-border` from `className="border-t border-sidebar-border p-2"`. Updated:
   ```
   "p-2"
   ```

4. The `<Separator>` component (line ~139) used between collapsed nav groups: replace with a simple spacer div. Change:
   ```tsx
   <Separator className="mx-2 my-1 bg-sidebar-border" />
   ```
   to:
   ```tsx
   <div className="my-2" />
   ```
   This uses spatial separation instead of a visual line.

5. Group labels (line ~134) already use `text-xs font-semibold text-muted-foreground uppercase tracking-wider` — verify this matches the UI-SPEC label typography. It does, so no change needed.

6. Remove the `Separator` import from the import block since it is no longer used:
   ```tsx
   // Remove this line:
   import { Separator } from "@/components/ui/separator"
   ```

**top-bar.tsx changes:**

1. On the `<header>` element, remove `border-b` from `className="h-14 border-b bg-background flex items-center px-4 md:px-6 shrink-0"`. Updated:
   ```
   "h-14 bg-background flex items-center px-4 md:px-6 shrink-0"
   ```
  </action>
  <verify>
    <automated>npx cypress run --spec cypress/e2e/dashboard.cy.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - sidebar.tsx aside element className does NOT contain `border-r` or `border-sidebar-border`
    - sidebar.tsx collapse toggle div className does NOT contain `border-b`
    - sidebar.tsx user section div className does NOT contain `border-t`
    - sidebar.tsx does NOT import `Separator` from `@/components/ui/separator`
    - sidebar.tsx does NOT contain `<Separator` JSX element
    - sidebar.tsx collapsed group separator is `<div className="my-2" />`
    - top-bar.tsx header className does NOT contain `border-b`
    - Dashboard Cypress test passes
  </acceptance_criteria>
  <done>Sidebar has no visible borders — separation is purely tonal. Top bar has no bottom border. Collapsed sidebar groups use spatial separation instead of Separator line.</done>
</task>

<task type="auto">
  <name>Task 2: Update AppShell content area surface</name>
  <files>src/components/layout/app-shell.tsx</files>
  <read_first>
    - src/components/layout/app-shell.tsx (current: main has p-4 md:p-6, no explicit bg class)
    - .planning/phases/09-design-system-overhaul-apply-sovereign-ledger-design-system-across-the-entire-app/09-UI-SPEC.md (Surface Tier Usage section)
  </read_first>
  <action>
In `app-shell.tsx`, update the `<main>` element to explicitly set the base surface tier background:

Change:
```tsx
<main className="flex-1 overflow-auto p-4 md:p-6">
```
To:
```tsx
<main className="flex-1 overflow-auto bg-background p-4 md:p-6">
```

This explicitly sets the "desk" surface tier on the content area, making the tonal contrast with the sidebar (bg-sidebar = oklch 0.96) intentional and clear. While `bg-background` is inherited from `body`, being explicit here documents the surface tier hierarchy.

No other changes to app-shell.tsx. The Sheet for mobile sidebar is correct as-is (it will inherit the glassmorphism from Plan 02's sheet.tsx changes).

After both tasks, enable the design-system.cy.ts tests that were skipped with "enable after Plan 03" comments. Update `cypress/e2e/design-system.cy.ts`:
- Change `it.skip(` to `it(` for the sidebar border-r test
- Change `it.skip(` to `it(` for the top-bar border-b test
  </action>
  <verify>
    <automated>npx cypress run --spec cypress/e2e/design-system.cy.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - app-shell.tsx main element className contains `bg-background`
    - cypress/e2e/design-system.cy.ts sidebar border test is NOT skipped (no `it.skip` for sidebar border test)
    - cypress/e2e/design-system.cy.ts top-bar border test is NOT skipped (no `it.skip` for top-bar border test)
    - Design system Cypress tests pass including border removal assertions
  </acceptance_criteria>
  <done>AppShell content area explicitly uses bg-background surface tier. Sidebar and top-bar border removal tests are enabled and passing.</done>
</task>

</tasks>

<verification>
npx cypress run --spec cypress/e2e/design-system.cy.ts
npx cypress run --spec cypress/e2e/dashboard.cy.ts
</verification>

<success_criteria>
- Sidebar has zero visible borders (no border-r, border-b, border-t)
- Top bar has no border-b
- Tonal separation between sidebar (oklch 0.96) and content (oklch 0.98) provides visual distinction
- Collapsed sidebar groups use spatial dividers instead of line separators
- Content area explicitly uses bg-background
- All Cypress tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-design-system-overhaul-apply-sovereign-ledger-design-system-across-the-entire-app/09-03-SUMMARY.md`
</output>
