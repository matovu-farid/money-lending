---
phase: 09-design-system-overhaul
plan: 04
type: execute
wave: 4
depends_on: ["09-03"]
files_modified:
  - src/components/dashboard/kpi-card.tsx
  - src/app/(app)/dashboard/page.tsx
  - src/app/(app)/customers/page.tsx
  - src/app/(app)/customers/[id]/page.tsx
  - src/app/(app)/loans/page.tsx
  - src/app/(app)/payments/PaymentsClient.tsx
  - src/app/(app)/payments/DailyCollectionsTab.tsx
  - src/app/(app)/payments/QuickRecordDialog.tsx
  - src/app/(app)/watchlist/page.tsx
autonomous: true
requirements:
  - DS-09
  - DS-10

must_haves:
  truths:
    - "All currency/numeric values on Dashboard, Customers, Loans, Payments, Watchlist use font-mono tabular-nums"
    - "All page h1 headings use text-2xl font-semibold tracking-tight"
    - "All page h1 headings have a subtitle in label typography (text-xs font-semibold uppercase tracking-wider text-muted-foreground)"
    - "KpiCard label uses label typography and value uses font-mono"
    - "Amount columns in tables are text-right font-mono tabular-nums"
  artifacts:
    - path: "src/components/dashboard/kpi-card.tsx"
      provides: "KPI card with Sovereign Ledger typography"
      contains: "font-mono"
    - path: "src/app/(app)/dashboard/page.tsx"
      provides: "Dashboard with Sovereign Ledger typography"
      contains: "tracking-tight"
  key_links:
    - from: "src/components/dashboard/kpi-card.tsx"
      to: "src/components/ui/card.tsx"
      via: "Card component (now rounded-lg, no ring)"
      pattern: "Card"
---

<objective>
Apply Sovereign Ledger typography and surface-tier patterns to core pages: Dashboard, Customers, Loans, Payments, and Watchlist. Add font-mono to all numeric values, tracking-tight to headings, label typography to subtitles/metadata.

Purpose: These 5 pages represent the primary user workflow. Applying typography here covers ~80% of daily usage.
Output: 9 updated page/component files with Sovereign Ledger typography
</objective>

<execution_context>
@/Users/faridmatovu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/faridmatovu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN.md
@.planning/phases/09-design-system-overhaul-apply-sovereign-ledger-design-system-across-the-entire-app/09-UI-SPEC.md
@.planning/phases/09-design-system-overhaul-apply-sovereign-ledger-design-system-across-the-entire-app/09-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: KpiCard + Dashboard page typography</name>
  <files>
    src/components/dashboard/kpi-card.tsx
    src/app/(app)/dashboard/page.tsx
  </files>
  <read_first>
    - src/components/dashboard/kpi-card.tsx (current: label is text-sm text-muted-foreground, value is text-2xl font-semibold)
    - src/app/(app)/dashboard/page.tsx (current: heading, formatUGX usage, activity feed, KPI grid)
    - .planning/phases/09-design-system-overhaul-apply-sovereign-ledger-design-system-across-the-entire-app/09-UI-SPEC.md (Typography section, Copywriting Contract)
  </read_first>
  <action>
**kpi-card.tsx changes:**

1. Label `<p>` — change from `text-sm text-muted-foreground` to `text-xs font-semibold uppercase tracking-wider text-muted-foreground` (label typography per UI-SPEC).

2. Value `<p>` — change from `text-2xl font-semibold` to `text-2xl font-semibold font-mono tracking-tight tabular-nums`. Keep the `cn()` wrapper with `valueClassName`.

3. Subtitle `<p>` — keep `text-sm text-muted-foreground` as-is (this is body text describing the metric).

4. Icon — keep `text-muted-foreground` (not Electric Blue — icons are decorative).

**dashboard/page.tsx changes:**

1. Find the page heading (likely `<h1>` or top-level heading element with `text-2xl font-semibold` or similar). Update to:
   ```tsx
   <h1 className="text-2xl font-semibold tracking-tight">Dashboard</h1>
   <p className="text-xs font-semibold uppercase tracking-wider text-muted-foreground mt-1">Portfolio health at a glance</p>
   ```
   If there is no explicit `<h1>`, add one at the top of the page content.

2. Find ALL `formatUGX()` usages — every element displaying a formatted currency value must have `font-mono tabular-nums` in its className. This includes:
   - KPI card values (handled via kpi-card.tsx changes above)
   - Activity feed amounts if displayed inline
   - Any other numeric displays

3. The `formatDetailValue` function returns formatted currency strings — ensure that any elements rendering its output have `font-mono tabular-nums` in their className.

4. Activity feed items — if any activity feed item shows a numeric value (amount, interest rate), ensure the element wrapping that value has `font-mono`. Customer names and descriptions stay in sans-serif (no font-mono).

5. Activity feed timestamp (relativeTime) — add `font-mono` to the element displaying relative time values.

Note: Do NOT add font-mono to customer names, status labels, descriptions, or section headings. Only numeric/temporal values.
  </action>
  <verify>
    <automated>npx cypress run --spec cypress/e2e/dashboard.cy.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - kpi-card.tsx label p has classes `text-xs font-semibold uppercase tracking-wider text-muted-foreground`
    - kpi-card.tsx value p has classes `font-mono tracking-tight tabular-nums`
    - dashboard/page.tsx contains `<h1` with `tracking-tight`
    - dashboard/page.tsx contains subtitle text "Portfolio health at a glance" with `uppercase tracking-wider`
    - dashboard/page.tsx all formatUGX output elements have `font-mono`
    - Dashboard Cypress test passes
  </acceptance_criteria>
  <done>KpiCard uses label typography for labels and font-mono for values. Dashboard has Sovereign Ledger heading + subtitle. All currency values use font-mono tabular-nums.</done>
</task>

<task type="auto">
  <name>Task 2: Customers, Loans, Payments, Watchlist page typography</name>
  <files>
    src/app/(app)/customers/page.tsx
    src/app/(app)/customers/[id]/page.tsx
    src/app/(app)/loans/page.tsx
    src/app/(app)/payments/PaymentsClient.tsx
    src/app/(app)/payments/DailyCollectionsTab.tsx
    src/app/(app)/payments/QuickRecordDialog.tsx
    src/app/(app)/watchlist/page.tsx
  </files>
  <read_first>
    - src/app/(app)/customers/page.tsx (current page heading and table structure)
    - src/app/(app)/customers/[id]/page.tsx (customer detail page)
    - src/app/(app)/loans/page.tsx (current page heading and table structure)
    - src/app/(app)/payments/PaymentsClient.tsx (tabs, filter bar, table)
    - src/app/(app)/payments/DailyCollectionsTab.tsx (daily summary, amounts)
    - src/app/(app)/payments/QuickRecordDialog.tsx (payment form, amounts)
    - src/app/(app)/watchlist/page.tsx (watchlist table)
    - .planning/phases/09-design-system-overhaul-apply-sovereign-ledger-design-system-across-the-entire-app/09-UI-SPEC.md (Typography, Copywriting Contract sections)
  </read_first>
  <action>
Apply the following pattern to EACH of the 7 files:

**Pattern A — Page heading + subtitle (apply to each page's main heading):**

For each page, find the primary heading and update it. If no `<h1>` exists, wrap the existing heading text in `<h1>`. Add a subtitle line below it.

| Page | Heading Text | Subtitle Text |
|------|-------------|--------------|
| customers/page.tsx | "Customers" | "Customer portfolio overview" |
| customers/[id]/page.tsx | (dynamic customer name — keep as-is) | Add subtitle "Customer profile" |
| loans/page.tsx | "Loans" | "Active and historical loans" |
| payments/PaymentsClient.tsx | "Payments" | "Payment history and collections" |
| watchlist/page.tsx | "Watchlist" | "Overdue and at-risk loans" |

Heading pattern:
```tsx
<h1 className="text-2xl font-semibold tracking-tight">{title}</h1>
<p className="text-xs font-semibold uppercase tracking-wider text-muted-foreground mt-1">{subtitle}</p>
```

Note: DailyCollectionsTab.tsx and QuickRecordDialog.tsx do NOT get page headings (they are sub-components within PaymentsClient).

**Pattern B — Numeric values get font-mono (apply to ALL 7 files):**

Search each file for elements displaying:
- Currency amounts (formatUGX, UGX, formatNumberWithCommas, amount, balance, principal, interest values)
- Percentages (interest rate, %)
- Counts (loan count, payment count)
- Timestamps / dates (formatted dates)
- Loan IDs / slugs

Add `font-mono tabular-nums` to the className of the element wrapping these values. If the value is in a `<TableCell>`, also add `text-right` for amount columns.

**Pattern C — Amount TableCell columns (apply to customers, loans, payments, watchlist pages):**

For tables showing financial data, ensure:
- Amount cells: `<TableCell className="text-right font-mono tabular-nums">`
- Amount headers: `<TableHead className="text-right">`

Do NOT apply font-mono to: customer names, loan status badges, descriptions, action buttons, or non-numeric metadata.

**Specific file notes:**

- **DailyCollectionsTab.tsx**: Summary cards showing daily totals must use `font-mono tabular-nums` for the total amount and payment count. Per-loan breakdown amounts also need `font-mono`.
- **QuickRecordDialog.tsx**: The amount input field stays sans-serif (it is an input, not a display). The success state showing "Payment of UGX X recorded" — the amount portion should use `font-mono`.
- **customers/[id]/page.tsx**: Loan summary amounts, payment history amounts — all need `font-mono`. Customer name stays sans.
- **watchlist/page.tsx**: Days overdue count and outstanding balance amount need `font-mono`.
  </action>
  <verify>
    <automated>npx cypress run --spec cypress/e2e/dashboard.cy.ts --spec cypress/e2e/customer-crud.cy.ts --spec cypress/e2e/loans-list.cy.ts --spec cypress/e2e/payments-list.cy.ts --spec cypress/e2e/watchlist.cy.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - customers/page.tsx contains `<h1` with `tracking-tight` and text "Customers"
    - customers/page.tsx contains subtitle "Customer portfolio overview" with `uppercase tracking-wider`
    - loans/page.tsx contains `<h1` with `tracking-tight` and text "Loans"
    - loans/page.tsx contains subtitle "Active and historical loans" with `uppercase tracking-wider`
    - PaymentsClient.tsx contains `<h1` with `tracking-tight` and text "Payments"
    - PaymentsClient.tsx contains subtitle "Payment history and collections" with `uppercase tracking-wider`
    - watchlist/page.tsx contains `<h1` with `tracking-tight` and text "Watchlist"
    - watchlist/page.tsx contains subtitle "Overdue and at-risk loans" with `uppercase tracking-wider`
    - All 7 files have `font-mono` applied to at least one numeric value element
    - No file applies `font-mono` to customer name elements
    - Cypress tests pass for all 5 pages
  </acceptance_criteria>
  <done>All core pages have Sovereign Ledger headings with subtitles. All numeric/currency values use font-mono tabular-nums. Amount table columns are right-aligned.</done>
</task>

</tasks>

<verification>
npx cypress run --spec cypress/e2e/dashboard.cy.ts
npx cypress run --spec cypress/e2e/customer-crud.cy.ts
npx cypress run --spec cypress/e2e/loans-list.cy.ts
npx cypress run --spec cypress/e2e/payments-list.cy.ts
npx cypress run --spec cypress/e2e/watchlist.cy.ts
</verification>

<success_criteria>
- All core pages (Dashboard, Customers, Loans, Payments, Watchlist) have Sovereign Ledger heading + subtitle
- KpiCard uses label typography for labels and font-mono for values
- All currency, percentage, count, and timestamp values use font-mono tabular-nums
- Amount columns in tables are right-aligned
- No font-mono on non-numeric text (names, descriptions, status labels)
- All relevant Cypress tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-design-system-overhaul-apply-sovereign-ledger-design-system-across-the-entire-app/09-04-SUMMARY.md`
</output>
