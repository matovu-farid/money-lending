---
phase: 09-design-system-overhaul
plan: 06
type: execute
wave: 5
depends_on: ["09-04"]
files_modified:
  - src/app/(app)/reports/page.tsx
  - src/app/(app)/reports/portfolio/PortfolioClient.tsx
  - src/app/(app)/reports/pnl/PnlClient.tsx
  - src/app/(app)/reports/balance-sheet/BalanceSheetClient.tsx
  - src/app/(app)/receipts/disbursement/[loanId]/page.tsx
  - src/app/(app)/receipts/repayment/[paymentId]/page.tsx
  - cypress/e2e/design-system.cy.ts
autonomous: true
requirements:
  - DS-12

must_haves:
  truths:
    - "All report pages have Sovereign Ledger heading + subtitle"
    - "All numeric values in reports use font-mono tabular-nums"
    - "Receipt pages render correctly with print token resets"
    - "All design-system.cy.ts tests are un-skipped and passing"
  artifacts:
    - path: "cypress/e2e/design-system.cy.ts"
      provides: "Fully enabled design system verification"
    - path: "src/app/(app)/reports/page.tsx"
      provides: "Reports index with Sovereign Ledger typography"
      contains: "tracking-tight"
  key_links:
    - from: "src/app/(app)/receipts/disbursement/[loanId]/page.tsx"
      to: "src/app/globals.css"
      via: "@media print token resets"
      pattern: "receipt-body"
---

<objective>
Apply Sovereign Ledger typography to reports and receipt pages, enable all design-system.cy.ts tests, and run full Cypress regression pass.

Purpose: Complete the design system rollout for all remaining pages and verify the entire application passes regression.
Output: 6 updated page files + fully-enabled design-system test suite + full regression green
</objective>

<execution_context>
@/Users/faridmatovu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/faridmatovu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN.md
@.planning/phases/09-design-system-overhaul-apply-sovereign-ledger-design-system-across-the-entire-app/09-UI-SPEC.md
@.planning/phases/09-design-system-overhaul-apply-sovereign-ledger-design-system-across-the-entire-app/09-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reports + Receipts typography</name>
  <files>
    src/app/(app)/reports/page.tsx
    src/app/(app)/reports/portfolio/PortfolioClient.tsx
    src/app/(app)/reports/pnl/PnlClient.tsx
    src/app/(app)/reports/balance-sheet/BalanceSheetClient.tsx
    src/app/(app)/receipts/disbursement/[loanId]/page.tsx
    src/app/(app)/receipts/repayment/[paymentId]/page.tsx
  </files>
  <read_first>
    - src/app/(app)/reports/page.tsx (reports index)
    - src/app/(app)/reports/portfolio/PortfolioClient.tsx (portfolio report)
    - src/app/(app)/reports/pnl/PnlClient.tsx (P&L report)
    - src/app/(app)/reports/balance-sheet/BalanceSheetClient.tsx (balance sheet)
    - src/app/(app)/receipts/disbursement/[loanId]/page.tsx (disbursement receipt)
    - src/app/(app)/receipts/repayment/[paymentId]/page.tsx (repayment receipt)
    - src/app/globals.css (verify @media print block has token resets from Plan 01)
    - .planning/phases/09-design-system-overhaul-apply-sovereign-ledger-design-system-across-the-entire-app/09-UI-SPEC.md (Print Layout Protection, Typography sections)
  </read_first>
  <action>
**Reports pages:**

| Page | Heading | Subtitle |
|------|---------|----------|
| reports/page.tsx | "Reports" | "Financial reporting and analytics" |
| portfolio/PortfolioClient.tsx | "Portfolio Report" | "Loan portfolio analysis" |
| pnl/PnlClient.tsx | "Profit & Loss" | "Revenue and expense summary" |
| balance-sheet/BalanceSheetClient.tsx | "Balance Sheet" | "Assets, liabilities, and equity" |

Apply Pattern A (heading + subtitle) and Pattern B (font-mono on all numeric values) to each report page. Report pages are heavily numeric — ensure ALL currency amounts, percentages, totals, and subtotals use `font-mono tabular-nums`.

**Receipt pages:**

For receipt pages, apply font-mono to all amount values in the receipt body. These pages use a `receipt-body` class for print layout.

Do NOT change the print-specific classes or structure. Only add `font-mono tabular-nums` to elements displaying amounts and `tracking-tight` to the receipt title heading.

Verify that the `@media print` block in globals.css (from Plan 01) has the token resets:
```css
@media print {
  :root {
    --background: oklch(1 0 0);
    --foreground: oklch(0 0 0);
  }
}
```
If this is missing, add it. If present, no action needed.
  </action>
  <verify>
    <automated>npx cypress run --spec cypress/e2e/dashboard.cy.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - reports/page.tsx contains `tracking-tight` and subtitle "Financial reporting and analytics"
    - PortfolioClient.tsx contains `tracking-tight` and `font-mono`
    - PnlClient.tsx contains `tracking-tight` and `font-mono`
    - BalanceSheetClient.tsx contains `tracking-tight` and `font-mono`
    - receipts/disbursement/[loanId]/page.tsx contains `font-mono` on amount values
    - receipts/repayment/[paymentId]/page.tsx contains `font-mono` on amount values
    - globals.css @media print block contains `--background: oklch(1 0 0)`
  </acceptance_criteria>
  <done>All report pages have Sovereign Ledger typography. Receipt pages have font-mono on amounts. Print layout token resets confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Enable all design-system tests + full Cypress regression pass</name>
  <files>cypress/e2e/design-system.cy.ts</files>
  <read_first>
    - cypress/e2e/design-system.cy.ts (current state — check which tests are still skipped)
  </read_first>
  <action>
**Enable all skipped design-system.cy.ts tests:**

Open `cypress/e2e/design-system.cy.ts` and change ALL remaining `it.skip(` to `it(`. Remove all "enable after Plan XX" comments. All tests should now be active:
- Token value tests (enabled since Plan 01)
- Card ring removal test (enabled since Plan 02)
- Sidebar border test (enabled since Plan 03)
- Top-bar border test (enabled since Plan 03)
- Typography/tracking-tight test on dashboard heading (enable now — Plan 04 completed)

Run the full design system test suite to verify everything passes.

Then run the FULL Cypress test suite (`npx cypress run`) to verify no regressions across the entire application from the design system changes.

If any tests fail:
1. Read the failing test file
2. Determine if the failure is due to a design system change (e.g., a test asserting a specific class name that was changed)
3. Update the test to match the new design system classes
4. Re-run until all tests pass

Common test fixes needed:
- Tests asserting `rounded-xl` on cards -> change to `rounded-lg`
- Tests asserting `ring-1` on cards -> remove assertion
- Tests asserting `border-b` on top-bar -> remove assertion
- Tests asserting `shadow-lg` on sheets -> remove assertion
- Tests asserting specific OKLCH color values -> update to new token values
- Tests asserting `font-medium` on table headers -> change to `font-semibold`

Do NOT change application code in this task — only fix test assertions.
  </action>
  <verify>
    <automated>npx cypress run 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - cypress/e2e/design-system.cy.ts does NOT contain `it.skip(`
    - All design-system.cy.ts tests pass
    - `npx cypress run` exits with code 0 (all tests pass)
    - No test file contains assertions for `rounded-xl` on card elements
    - No test file contains assertions for `ring-1 ring-foreground/10` on card elements
  </acceptance_criteria>
  <done>All design-system.cy.ts tests are enabled and passing. Full Cypress suite passes with zero failures. Phase 9 is complete.</done>
</task>

</tasks>

<verification>
npx cypress run
</verification>

<success_criteria>
- Every page in the application has Sovereign Ledger heading + subtitle
- Every numeric value in the application uses font-mono tabular-nums
- Receipt pages print correctly (white background, black text)
- All design-system.cy.ts tests enabled and passing
- Full Cypress suite passes with zero failures
- No regressions from design system changes
</success_criteria>

<output>
After completion, create `.planning/phases/09-design-system-overhaul-apply-sovereign-ledger-design-system-across-the-entire-app/09-06-SUMMARY.md`
</output>
