---
phase: 16-cypress-mobile-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cypress/e2e/dashboard.cy.ts
  - cypress/e2e/activity-feed.cy.ts
  - cypress/e2e/homepage-redirect.cy.ts
  - cypress/e2e/notifications.cy.ts
  - cypress/e2e/daily-collections.cy.ts
  - cypress/e2e/quick-record.cy.ts
  - cypress/e2e/admin-panel.cy.ts
  - cypress/e2e/auth-gate.cy.ts
  - cypress/e2e/design-system.cy.ts
  - cypress/e2e/registration.cy.ts
  - cypress/e2e/reports.cy.ts
  - cypress/e2e/transactions.cy.ts
autonomous: true
requirements: [TEST-02, TEST-03]

must_haves:
  truths:
    - "All 12 remaining spec files contain a mobile viewport context block with cy.viewport(390, 844)"
    - "All 29 existing Cypress spec files pass at default desktop viewport after all responsive changes"
    - "The full Cypress suite runs green with no failures"
  artifacts:
    - path: "cypress/e2e/dashboard.cy.ts"
      provides: "Mobile viewport context block"
      contains: "context(\"at mobile viewport"
    - path: "cypress/e2e/activity-feed.cy.ts"
      provides: "Mobile viewport context block"
      contains: "context(\"at mobile viewport"
    - path: "cypress/e2e/homepage-redirect.cy.ts"
      provides: "Mobile viewport context block"
      contains: "context(\"at mobile viewport"
    - path: "cypress/e2e/notifications.cy.ts"
      provides: "Mobile viewport context block"
      contains: "context(\"at mobile viewport"
    - path: "cypress/e2e/daily-collections.cy.ts"
      provides: "Mobile viewport context block"
      contains: "context(\"at mobile viewport"
    - path: "cypress/e2e/quick-record.cy.ts"
      provides: "Mobile viewport context block"
      contains: "context(\"at mobile viewport"
    - path: "cypress/e2e/admin-panel.cy.ts"
      provides: "Mobile viewport context block"
      contains: "context(\"at mobile viewport"
    - path: "cypress/e2e/auth-gate.cy.ts"
      provides: "Mobile viewport context block"
      contains: "context(\"at mobile viewport"
    - path: "cypress/e2e/design-system.cy.ts"
      provides: "Mobile viewport context block"
      contains: "context(\"at mobile viewport"
    - path: "cypress/e2e/registration.cy.ts"
      provides: "Mobile viewport context block"
      contains: "context(\"at mobile viewport"
    - path: "cypress/e2e/reports.cy.ts"
      provides: "Mobile viewport context block"
      contains: "context(\"at mobile viewport"
    - path: "cypress/e2e/transactions.cy.ts"
      provides: "Mobile viewport context block"
      contains: "context(\"at mobile viewport"
  key_links:
    - from: "all 12 spec files"
      to: "BottomTabBar component"
      via: "data-testid selectors in mobile context blocks"
      pattern: "data-testid='bottom-tab-bar'"
---

<objective>
Add mobile viewport Cypress coverage to the remaining 12 simpler spec files and validate the full suite passes at desktop viewport.

Purpose: Complete TEST-03 (mobile viewport blocks in all specs) and validate TEST-02 (all specs pass at desktop after responsive changes). These 12 specs need only page-render + navigation assertions at mobile — no complex table/form patterns.

Output: 12 existing spec files updated with `context("at mobile viewport (390x844)")` blocks, and full suite regression confirmed green.
</objective>

<execution_context>
@/Users/faridmatovu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/faridmatovu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-cypress-mobile-coverage/16-RESEARCH.md
@.planning/phases/16-cypress-mobile-coverage/16-UI-SPEC.md

<interfaces>
<!-- Canonical mobile viewport pattern for simple pages (render + nav only). -->

From cypress/e2e/mobile-navigation.cy.ts:
```typescript
// Tab bar assertion (position:fixed — use exist + CSS, not be.visible)
cy.get("[data-testid='bottom-tab-bar']").should("exist")
  .should("have.css", "display", "flex")
// Sidebar hidden at mobile
cy.get("[data-testid='sidebar-nav']").should("not.be.visible")
```

From cypress/e2e/responsive-layouts.cy.ts (table page — use ONLY if page has ResponsiveTable):
```typescript
cy.get("[data-slot='table-container']").should("not.be.visible")
cy.get("[data-testid='data-row']").filter(":visible").should("have.length.gte", 1)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mobile viewport blocks to 6 page specs (dashboard, activity, homepage, notifications, daily-collections, quick-record)</name>
  <files>
    cypress/e2e/dashboard.cy.ts
    cypress/e2e/activity-feed.cy.ts
    cypress/e2e/homepage-redirect.cy.ts
    cypress/e2e/notifications.cy.ts
    cypress/e2e/daily-collections.cy.ts
    cypress/e2e/quick-record.cy.ts
  </files>
  <read_first>
    cypress/e2e/dashboard.cy.ts
    cypress/e2e/activity-feed.cy.ts
    cypress/e2e/homepage-redirect.cy.ts
    cypress/e2e/notifications.cy.ts
    cypress/e2e/daily-collections.cy.ts
    cypress/e2e/quick-record.cy.ts
    cypress/e2e/mobile-navigation.cy.ts
    cypress/e2e/responsive-layouts.cy.ts
  </read_first>
  <action>
For each of the 6 spec files, add a `context("at mobile viewport (390x844)")` block INSIDE the existing top-level `describe()`, AFTER all existing `it()` blocks.

**Per-file specifics:**

1. **dashboard.cy.ts** — Page URL: `/dashboard`. Mobile block tests:
   - (a) "renders dashboard at mobile with tab bar" — visit `/dashboard`, assert h1 or "Dashboard" heading visible, assert `[data-testid='bottom-tab-bar']` exists with `display: flex`, assert `[data-testid='sidebar-nav']` not visible.
   - (b) "KPI cards are visible at mobile" — assert KPI card text is present (e.g., `cy.contains("Loans Outstanding").should("be.visible")`). At least 2-3 KPI cards checked.

2. **activity-feed.cy.ts** — Page URL: `/dashboard` (activity feed is on dashboard). Mobile block:
   - (a) "renders activity feed section at mobile" — visit `/dashboard`, set viewport, assert "Activity" or "Recent Activity" heading/section exists, assert tab bar visible, sidebar hidden.

3. **homepage-redirect.cy.ts** — Page URL: `/`. This spec tests redirect from `/` to `/dashboard`. Mobile block:
   - (a) "redirects to dashboard at mobile viewport" — `cy.viewport(390, 844)`, visit `/`, assert URL includes `/dashboard`. This is viewport-agnostic behavior but the mobile context block is required for TEST-03 compliance. Also assert tab bar exists after redirect.

4. **notifications.cy.ts** — Page URL: whatever page the notification bell is on (likely `/dashboard` or global). Read file to determine. Mobile block:
   - (a) "notification bell is accessible at mobile" — visit the page, assert notification trigger/bell element is visible at mobile, assert tab bar visible, sidebar hidden.

5. **daily-collections.cy.ts** — Page URL: `/collections` or `/daily-collections` (read file). Mobile block:
   - (a) "renders page at mobile with tab bar" — standard render + nav assertions.
   - (b) If page has a table with data-row, add `.filter(":visible")` card assertion. If no table, just render check.

6. **quick-record.cy.ts** — Page URL: read file to determine. Mobile block:
   - (a) "renders page at mobile with tab bar" — standard render + nav assertions.
   - (b) If page has form elements, verify they are visible at mobile.

**Standard context block template:**
```typescript
context("at mobile viewport (390x844)", () => {
  beforeEach(() => {
    cy.viewport(390, 844)
  })

  it("renders page at mobile and shows tab bar", () => {
    cy.visit("/<url>")
    cy.get("[data-testid='bottom-tab-bar']").should("exist")
      .should("have.css", "display", "flex")
    cy.get("[data-testid='sidebar-nav']").should("not.be.visible")
  })
})
```

**CRITICAL:** Do NOT modify any existing `it()` blocks. Only ADD the new `context()` block at the end of the `describe()`.
  </action>
  <verify>
    <automated>npx cypress run --spec cypress/e2e/dashboard.cy.ts,cypress/e2e/activity-feed.cy.ts,cypress/e2e/homepage-redirect.cy.ts,cypress/e2e/notifications.cy.ts,cypress/e2e/daily-collections.cy.ts,cypress/e2e/quick-record.cy.ts</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 6 files contains `context("at mobile viewport (390x844)"`
    - Each of the 6 files contains `cy.viewport(390, 844)` inside a beforeEach within the context block
    - Each of the 6 files contains `data-testid='bottom-tab-bar'` assertion
    - Each of the 6 files contains `sidebar-nav.*not.be.visible` assertion
    - homepage-redirect.cy.ts mobile block asserts redirect to /dashboard
    - dashboard.cy.ts mobile block asserts at least one KPI card visible
    - All 6 spec files pass: cypress run exits 0
  </acceptance_criteria>
  <done>All 6 page spec files have mobile viewport context blocks with tab bar and sidebar assertions, all passing</done>
</task>

<task type="auto">
  <name>Task 2: Add mobile viewport blocks to 6 remaining specs (admin, auth, design-system, registration, reports, transactions)</name>
  <files>
    cypress/e2e/admin-panel.cy.ts
    cypress/e2e/auth-gate.cy.ts
    cypress/e2e/design-system.cy.ts
    cypress/e2e/registration.cy.ts
    cypress/e2e/reports.cy.ts
    cypress/e2e/transactions.cy.ts
  </files>
  <read_first>
    cypress/e2e/admin-panel.cy.ts
    cypress/e2e/auth-gate.cy.ts
    cypress/e2e/design-system.cy.ts
    cypress/e2e/registration.cy.ts
    cypress/e2e/reports.cy.ts
    cypress/e2e/transactions.cy.ts
    cypress/e2e/mobile-navigation.cy.ts
  </read_first>
  <action>
For each of the 6 spec files, add a `context("at mobile viewport (390x844)")` block INSIDE the existing top-level `describe()`, AFTER all existing `it()` blocks.

**Per-file specifics:**

1. **admin-panel.cy.ts** — Read to determine page URL (likely `/admin`). Mobile block:
   - (a) "renders admin panel at mobile with tab bar" — standard render + nav.
   - (b) If admin page has table content, add `.filter(":visible")` assertion. If not, just render check.

2. **auth-gate.cy.ts** — This spec tests unauthenticated access behavior. Mobile block:
   - (a) "redirects unauthenticated user at mobile" — `cy.viewport(390, 844)`, visit a protected route without login, assert redirect to login/auth page. This is viewport-agnostic but the context block is required for TEST-03.
   - NOTE: auth-gate spec may NOT have `cy.registerAndLogin()` in beforeEach (it tests unauthenticated flow). Read the file carefully. If it already logs in for some tests, add the mobile context inside the appropriate describe block. If it tests unauthenticated redirects, the mobile block should test the same redirect at mobile viewport.

3. **design-system.cy.ts** — Read to determine page URL (likely `/design-system` or similar). Mobile block:
   - (a) "renders design system page at mobile" — standard render + nav. If the page shows component examples, just verify the page loads.

4. **registration.cy.ts** — Page URL: `/register` or `/auth/register`. Mobile block:
   - (a) "renders registration form at mobile" — visit registration page, assert form fields visible (name, email, password inputs), assert page heading visible.
   - NOTE: Registration page may not show tab bar (unauthenticated). Read the file. If the page is pre-auth, skip tab bar assertion and just verify the page renders at mobile.

5. **reports.cy.ts** — Page URL: `/reports`. Mobile block:
   - (a) "renders reports page at mobile with tab bar" — standard render + nav.
   - (b) If reports page has table content, add `.filter(":visible")` card assertion. If it shows report links/cards, just verify they are visible.

6. **transactions.cy.ts** — Page URL: `/transactions` or similar. Mobile block:
   - (a) "renders transactions page at mobile with tab bar" — standard render + nav.
   - (b) If page has ResponsiveTable with data-row, add `.filter(":visible")` assertion.

**CRITICAL patterns:**
- Tab bar: `.should("exist").should("have.css", "display", "flex")` — NOT `.should("be.visible")`
- Sidebar: `.should("not.be.visible")`
- Read each file FIRST to understand its beforeEach setup (some specs like auth-gate may have different auth patterns)
- For pages that use ResponsiveTable (have `data-row` assertions in existing tests), add `.filter(":visible")` at mobile
- Do NOT modify any existing `it()` blocks. Only ADD the new `context()` block.
  </action>
  <verify>
    <automated>npx cypress run --spec cypress/e2e/admin-panel.cy.ts,cypress/e2e/auth-gate.cy.ts,cypress/e2e/design-system.cy.ts,cypress/e2e/registration.cy.ts,cypress/e2e/reports.cy.ts,cypress/e2e/transactions.cy.ts</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 6 files contains `context("at mobile viewport (390x844)"` (or equivalent mobile context for auth-gate/registration if unauthenticated)
    - Each of the 6 files contains `cy.viewport(390, 844)`
    - admin-panel.cy.ts, reports.cy.ts, transactions.cy.ts contain `data-testid='bottom-tab-bar'` assertion
    - auth-gate.cy.ts contains a mobile viewport test (redirect or page render at 390px)
    - registration.cy.ts contains a mobile viewport test (form renders at 390px)
    - All 6 spec files pass: cypress run exits 0
  </acceptance_criteria>
  <done>All 6 remaining spec files have mobile viewport context blocks appropriate to their page type (authenticated with tab bar, or unauthenticated with render check), all passing</done>
</task>

<task type="auto">
  <name>Task 3: Full suite desktop regression validation (TEST-02)</name>
  <files></files>
  <read_first>
    cypress.config.ts
  </read_first>
  <action>
Run the complete Cypress suite to validate TEST-02: all 29+ spec files pass at default (desktop) viewport after all responsive changes from Phases 12-15 and the mobile viewport blocks added in this phase.

1. Run `npx cypress run` (no `--spec` flag — runs ALL specs).
2. If any spec fails:
   - Read the failing spec file
   - Diagnose whether the failure is in an existing desktop test or a newly added mobile context block
   - Fix the failure (adjust assertions, fix selectors, handle timing issues)
   - Re-run the failing spec individually: `npx cypress run --spec cypress/e2e/<failing-spec>.cy.ts`
   - After individual fix confirmed, re-run full suite
3. Repeat until full suite is green.

**Expected outcome:** All spec files pass at their default (desktop) viewport AND within their mobile viewport context blocks. Zero failures.

**If flaky tests are encountered:** Add `{ timeout: 10000 }` to the specific assertion or add `cy.wait()` only as a last resort. Prefer increasing assertion timeouts over arbitrary waits.

**Do NOT:**
- Modify cypress.config.ts global viewport settings
- Skip or `.skip()` any failing test
- Remove existing test assertions
  </action>
  <verify>
    <automated>npx cypress run</automated>
  </verify>
  <acceptance_criteria>
    - `npx cypress run` exits with code 0
    - Output shows all spec files passing (0 failures)
    - No `.skip()` added to any test that previously passed
    - cypress.config.ts is unmodified
  </acceptance_criteria>
  <done>Full Cypress suite (all 29+ spec files) passes green at desktop viewport with zero failures, confirming TEST-02 compliance</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:
1. `npx cypress run` exits 0 — full suite green (TEST-02)
2. `grep -rl "at mobile viewport" cypress/e2e/*.cy.ts | wc -l` returns 29 (all 25 updated + 4 already had them)
3. `ls cypress/e2e/tab-bar.cy.ts` confirms file exists (TEST-04, covered by Plan 01)
</verification>

<success_criteria>
- All 12 spec files updated with mobile viewport context blocks
- Full Cypress suite passes at desktop viewport (TEST-02)
- Zero test failures, zero skipped tests
</success_criteria>

<output>
After completion, create `.planning/phases/16-cypress-mobile-coverage/16-02-SUMMARY.md`
</output>
