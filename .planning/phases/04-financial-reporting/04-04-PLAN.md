---
phase: 04-financial-reporting
plan: 04
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/app/(app)/creditors/page.tsx
  - src/app/(app)/creditors/new/page.tsx
  - src/app/(app)/creditors/[id]/page.tsx
  - src/app/(app)/creditors/[id]/CreditorProfileClient.tsx
  - src/app/(app)/creditors/[id]/AddInvestmentDialog.tsx
  - src/app/(app)/creditors/[id]/RecordRepaymentDialog.tsx
  - src/components/layout/sidebar.tsx
  - cypress/e2e/creditors.cy.ts
autonomous: true
requirements:
  - CRED-01
  - CRED-02
  - CRED-04
  - CRED-05
  - CRED-06

must_haves:
  truths:
    - "User can view a list of all creditors"
    - "User can register a new creditor with name, contact, address, and initial investment"
    - "User can view a creditor profile with KPI cards and investment history"
    - "User can add an investment to a creditor via dialog"
    - "User can record a repayment to a creditor via dialog"
    - "Creditor dashboard shows totalInvested, interestAccrued, repaymentsMade, outstandingBalance"
    - "Sidebar Creditors link is enabled and navigable"
    - "Cypress creditors smoke spec exists and runs"
  artifacts:
    - path: "src/app/(app)/creditors/page.tsx"
      provides: "Creditor list page"
      contains: "Creditors"
    - path: "src/app/(app)/creditors/new/page.tsx"
      provides: "Creditor registration form"
      contains: "Add Creditor"
    - path: "src/app/(app)/creditors/[id]/page.tsx"
      provides: "Creditor profile server component"
      contains: "getCreditorDashboard"
    - path: "src/app/(app)/creditors/[id]/CreditorProfileClient.tsx"
      provides: "Client island for creditor profile interactivity"
      contains: "use client"
    - path: "cypress/e2e/creditors.cy.ts"
      provides: "E2E smoke spec for creditor registration and dashboard"
      contains: "creditors"
  key_links:
    - from: "src/app/(app)/creditors/page.tsx"
      to: "src/services/creditor.service.ts"
      via: "Effect.runPromise(listCreditors())"
      pattern: "listCreditors"
    - from: "src/app/(app)/creditors/[id]/page.tsx"
      to: "src/services/creditor.service.ts"
      via: "Effect.runPromise(getCreditorDashboard(id))"
      pattern: "getCreditorDashboard"
    - from: "src/app/(app)/creditors/new/page.tsx"
      to: "src/app/(app)/creditors/actions.ts"
      via: "Server Action form submission"
      pattern: "createCreditorAction"
---

<objective>
Build the creditor UI pages: list, registration form, and profile with dashboard KPIs and investment/repayment dialogs. Create Cypress smoke spec for creditor flows.

Purpose: These pages fulfill CRED-01 (register creditor), CRED-02 (view/edit profile), CRED-04 (record repayments), CRED-05 (creditor dashboard), and CRED-06 (system-wide capital view on list page). The UI follows the Server Component + client island pattern established in Phases 2-3.
Output: Creditor list page, registration form, profile with KPI cards and tabs, investment/repayment dialogs, sidebar enabled, Cypress smoke spec.
</objective>

<execution_context>
@/Users/faridmatovu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/faridmatovu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-financial-reporting/04-CONTEXT.md
@.planning/phases/04-financial-reporting/04-UI-SPEC.md
@.planning/phases/04-financial-reporting/04-02-SUMMARY.md

<interfaces>
<!-- Creditor service exports (from Plan 02) -->
export const createCreditor: (input: CreateCreditorInput, actorId: string) => Effect.Effect<Creditor, DatabaseError>
export const updateCreditor: (id: string, input: UpdateCreditorInput, actorId: string) => Effect.Effect<Creditor, DatabaseError | CreditorNotFound>
export const getCreditor: (id: string) => Effect.Effect<Creditor, DatabaseError | CreditorNotFound>
export const listCreditors: () => Effect.Effect<Creditor[], DatabaseError>
export const addInvestment: (input: AddInvestmentInput, actorId: string) => Effect.Effect<CreditorInvestment, DatabaseError | CreditorNotFound>
export const recordCreditorRepayment: (input: RecordCreditorRepaymentInput, actorId: string) => Effect.Effect<CreditorRepayment, DatabaseError | InvestmentNotFound>
export const getCreditorDashboard: (creditorId: string) => Effect.Effect<CreditorDashboard, DatabaseError | CreditorNotFound>
export const getSystemCapital: () => Effect.Effect<{totalInvested, totalInterestAccrued, totalRepaymentsMade, totalOutstanding}, DatabaseError>

<!-- Server Actions (from Plan 02) -->
createCreditorAction(formData), updateCreditorAction(id, formData), addInvestmentAction(formData), recordCreditorRepaymentAction(formData)

<!-- Existing KpiCard component pattern -->
<KpiCard label="string" value="string" icon={LucideIcon} />

<!-- UI-SPEC Copywriting -->
Page title: "Creditors"
Primary CTA: "Add Creditor"
Empty state heading: "No creditors yet"
Empty state body: "Register your first creditor to start tracking invested capital."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creditor list page and registration form</name>
  <files>
    src/app/(app)/creditors/page.tsx
    src/app/(app)/creditors/new/page.tsx
    src/components/layout/sidebar.tsx
  </files>
  <read_first>
    - /Users/faridmatovu/projects/money-lending/src/app/(app)/customers/page.tsx
    - /Users/faridmatovu/projects/money-lending/src/app/(app)/customers/new/page.tsx
    - /Users/faridmatovu/projects/money-lending/src/components/layout/sidebar.tsx
    - /Users/faridmatovu/projects/money-lending/src/app/(app)/creditors/actions.ts
  </read_first>
  <action>
**1. Create `src/app/(app)/creditors/page.tsx`** — Server Component:
- Import `listCreditors` from `@/services/creditor.service`, `Effect` from `effect`
- Also import `getSystemCapital` from `@/services/creditor.service` — this fulfills CRED-06 (system-wide capital view) by displaying aggregate KPIs on the creditors list page
- Call `Effect.runPromise(listCreditors())` to fetch data
- Call `Effect.runPromise(getSystemCapital())` to fetch aggregate capital data
- Page layout follows customer list pattern: page title "Creditors" + "Add Creditor" button (Link to /creditors/new) + table
- Table columns: Name, Contact, Address, Date Added, Actions (View link)
- Empty state: heading "No creditors yet", body "Register your first creditor to start tracking invested capital."
- Use existing `table` shadcn component for the list
- 4-column KPI grid at top (matches Phase 3 dashboard pattern):
  ```
  <div class="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4">
  ```
  Show system-wide capital KPIs using `getSystemCapital()` — Total Invested, Total Interest Accrued, Total Repayments, Total Outstanding

**2. Create `src/app/(app)/creditors/new/page.tsx`** — Server Component with form:
- Registration form following customer registration pattern
- Field order per UI-SPEC: Name, Contact, Address, then Investment section header, Amount Invested (UGX prefix), Interest Rate (% suffix, default pre-filled), Date
- Submit calls `createCreditorAction` Server Action + `addInvestmentAction` if investment fields provided
- After success redirect to `/creditors` via `redirect()` from `next/navigation`
- Amount field: UGX prefix display, numeric input
- Interest Rate: number input with % suffix, default value from system settings if available
- "Add Creditor" button, disabled while submitting, shows spinner (use useFormStatus or client component wrapper)

**3. Update `src/components/layout/sidebar.tsx`:**
- Find the Creditors nav item with `disabled: true` and change to `disabled: false` (or remove the disabled property)
- Find "Expenses & Income" nav item and change to `disabled: false`
- Find "Reports" nav item and change to `disabled: false`
- This enables all Phase 4 navigation items at once
  </action>
  <verify>
    <automated>cd /Users/faridmatovu/projects/money-lending && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(app)/creditors/page.tsx contains `listCreditors` call
    - src/app/(app)/creditors/page.tsx contains `getSystemCapital` call (CRED-06)
    - src/app/(app)/creditors/page.tsx contains "No creditors yet"
    - src/app/(app)/creditors/page.tsx contains "Add Creditor"
    - src/app/(app)/creditors/new/page.tsx contains `createCreditorAction`
    - src/app/(app)/creditors/new/page.tsx contains field for "amount" and "interestRateMonthly"
    - src/components/layout/sidebar.tsx does NOT contain `disabled: true` for Creditors, Expenses, or Reports
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Creditor list page shows all creditors with system capital KPIs (CRED-06), registration form captures creditor + initial investment, sidebar links enabled</done>
</task>

<task type="auto">
  <name>Task 2: Creditor profile page with dashboard KPIs and investment/repayment dialogs</name>
  <files>
    src/app/(app)/creditors/[id]/page.tsx
    src/app/(app)/creditors/[id]/CreditorProfileClient.tsx
    src/app/(app)/creditors/[id]/AddInvestmentDialog.tsx
    src/app/(app)/creditors/[id]/RecordRepaymentDialog.tsx
  </files>
  <read_first>
    - /Users/faridmatovu/projects/money-lending/src/app/(app)/loans/[id]/page.tsx
    - /Users/faridmatovu/projects/money-lending/src/app/(app)/loans/[id]/LoanDetailClient.tsx
    - /Users/faridmatovu/projects/money-lending/src/app/(app)/creditors/actions.ts
    - /Users/faridmatovu/projects/money-lending/src/components/shared/KpiCard.tsx
  </read_first>
  <action>
**1. Create `src/app/(app)/creditors/[id]/page.tsx`** — Server Component:
- Fetches creditor data and dashboard via `Effect.runPromise(getCreditor(id))` and `Effect.runPromise(getCreditorDashboard(id))`
- Passes data as props to `CreditorProfileClient`
- Page heading: `{creditorName}` (dynamic)
- 4 KPI cards at top (reuse KpiCard component):
  - "Total Invested" — dashboard.totalInvested (UGX formatted)
  - "Interest Accrued" — dashboard.interestAccrued
  - "Repayments Made" — dashboard.repaymentsMade
  - "Outstanding Balance" — dashboard.outstandingBalance
- Grid: `<div class="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4">`

**2. Create `src/app/(app)/creditors/[id]/CreditorProfileClient.tsx`** — "use client":
- Tabs component (shadcn tabs) with two tabs: "Investments" and "Repayments"
- Investments tab: Table of investments with columns: Date, Amount, Rate, Principal Balance, Interest Accrued, Total Repaid. "Add Investment" button triggers AddInvestmentDialog.
- Repayments tab: Table of all repayments across investments. Columns: Date, Amount, Interest Portion, Principal Portion, Balance After.
- Empty state for investments: "No investments recorded for this creditor."
- "Add Investment" and "Record Repayment" buttons in header area

**3. Create `src/app/(app)/creditors/[id]/AddInvestmentDialog.tsx`** — "use client":
- Dialog (shadcn dialog) triggered by "Add Investment" button
- Fields: Amount (UGX prefix), Interest Rate (% suffix), Date (date input)
- Submit calls `addInvestmentAction` Server Action
- On success: dialog closes, call `router.refresh()` to refresh server data
- Submit button: "Add Investment", disabled while submitting

**4. Create `src/app/(app)/creditors/[id]/RecordRepaymentDialog.tsx`** — "use client":
- Dialog triggered by "Record Repayment" button
- Shows current outstanding balance as read-only reference above form
- Fields: Investment (select dropdown of active investments), Amount (UGX prefix), Date
- Submit calls `recordCreditorRepaymentAction` Server Action (fulfills CRED-04)
- On success: dialog closes, call `router.refresh()`
- Submit button: "Record Repayment", disabled while submitting
  </action>
  <verify>
    <automated>cd /Users/faridmatovu/projects/money-lending && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(app)/creditors/[id]/page.tsx contains `getCreditorDashboard`
    - src/app/(app)/creditors/[id]/page.tsx contains `KpiCard`
    - src/app/(app)/creditors/[id]/CreditorProfileClient.tsx contains `"use client"`
    - src/app/(app)/creditors/[id]/CreditorProfileClient.tsx contains `Tabs` or `tabs`
    - src/app/(app)/creditors/[id]/AddInvestmentDialog.tsx contains `addInvestmentAction`
    - src/app/(app)/creditors/[id]/AddInvestmentDialog.tsx contains `router.refresh()`
    - src/app/(app)/creditors/[id]/RecordRepaymentDialog.tsx contains `recordCreditorRepaymentAction`
    - src/app/(app)/creditors/[id]/RecordRepaymentDialog.tsx contains `Outstanding` or `outstandingBalance`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Creditor profile page shows KPI dashboard with investment history, add investment dialog, and record repayment dialog (CRED-04) — all interactive and refreshing on success</done>
</task>

<task type="auto">
  <name>Task 3: Cypress creditor smoke spec</name>
  <files>
    cypress/e2e/creditors.cy.ts
  </files>
  <read_first>
    - /Users/faridmatovu/projects/money-lending/cypress/e2e/customer-crud.cy.ts
    - /Users/faridmatovu/projects/money-lending/cypress/e2e/dashboard.cy.ts
  </read_first>
  <action>
Create `cypress/e2e/creditors.cy.ts` as a smoke spec covering the core creditor registration and dashboard flow. Follow the existing Cypress spec patterns from `customer-crud.cy.ts`.

```typescript
describe("Creditors", () => {
  beforeEach(() => {
    // Login using the same pattern as other specs
    cy.login() // or whatever auth helper is used in existing specs
  })

  it("shows the creditors list page", () => {
    cy.visit("/creditors")
    cy.contains("Creditors").should("be.visible")
    // Should show system capital KPI cards
    cy.contains("Total Invested").should("be.visible")
  })

  it("can navigate to the add creditor form", () => {
    cy.visit("/creditors")
    cy.contains("Add Creditor").click()
    cy.url().should("include", "/creditors/new")
  })

  it("can register a new creditor with initial investment", () => {
    cy.visit("/creditors/new")
    cy.get('input[name="name"]').type("Test Creditor")
    cy.get('input[name="contact"]').type("0700000000")
    cy.get('input[name="address"]').type("Kampala, Uganda")
    cy.get('input[name="amount"]').type("5000000")
    cy.get('input[name="interestRateMonthly"]').clear().type("10")
    // Submit form
    cy.contains("Add Creditor").click()
    // Should redirect to creditors list
    cy.url().should("include", "/creditors")
    cy.contains("Test Creditor").should("be.visible")
  })

  it("can view creditor profile with KPI dashboard", () => {
    cy.visit("/creditors")
    // Click on the first creditor (from previous test or seeded data)
    cy.contains("View").first().click()
    // Should show KPI cards
    cy.contains("Total Invested").should("be.visible")
    cy.contains("Outstanding Balance").should("be.visible")
    // Should show tabs
    cy.contains("Investments").should("be.visible")
    cy.contains("Repayments").should("be.visible")
  })
})
```

Adapt the selectors and auth pattern to match what existing specs use (check `customer-crud.cy.ts` for the exact login helper, selector patterns, and assertion style). The spec should be a basic smoke test — not exhaustive coverage — to satisfy the wave-end Cypress suite requirement.
  </action>
  <verify>
    <automated>cd /Users/faridmatovu/projects/money-lending && npx tsc --noEmit --project cypress/tsconfig.json 2>&1 | head -10 || npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - cypress/e2e/creditors.cy.ts exists
    - cypress/e2e/creditors.cy.ts contains `describe("Creditors"`
    - cypress/e2e/creditors.cy.ts contains `cy.visit("/creditors")`
    - cypress/e2e/creditors.cy.ts contains test for creditor registration
    - cypress/e2e/creditors.cy.ts contains test for creditor profile view
  </acceptance_criteria>
  <done>Cypress creditor smoke spec exists and covers registration flow, list page, and profile dashboard — pnpm cypress:run will find and execute it</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `pnpm test` passes (no regressions)
- All creditor pages exist and compile
- Sidebar shows Creditors, Expenses & Income, Reports as enabled links
- `cypress/e2e/creditors.cy.ts` exists for wave-end Cypress suite
</verification>

<success_criteria>
Complete creditor management UI: users can browse creditors with system capital KPIs, register new ones with initial investments, view creditor profile with live KPI dashboard, add investments, and record repayments. Cypress smoke spec covers the happy path. All using established Server Component + client island patterns.
</success_criteria>

<output>
After completion, create `.planning/phases/04-financial-reporting/04-04-SUMMARY.md`
</output>
