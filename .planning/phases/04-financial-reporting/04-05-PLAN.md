---
phase: 04-financial-reporting
plan: 05
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - src/app/(app)/expenses/page.tsx
  - src/app/(app)/expenses/ExpenseListClient.tsx
  - src/app/(app)/income/page.tsx
  - src/app/(app)/income/IncomeListClient.tsx
  - src/app/(app)/transactions/page.tsx
  - src/app/(app)/transactions/TransactionLogClient.tsx
autonomous: true
requirements:
  - FINC-01
  - FINC-02
  - FINC-03

must_haves:
  truths:
    - "User can view, add, and delete expenses with configurable categories"
    - "User can view, add, and delete income entries with configurable categories"
    - "Transaction log shows all debit/credit entries with filtering by type, category, and date range"
    - "User can add custom categories via inline popover"
    - "Categories with transactions cannot be deleted (tooltip explains why)"
  artifacts:
    - path: "src/app/(app)/expenses/page.tsx"
      provides: "Expenses list page"
      contains: "Expenses"
    - path: "src/app/(app)/income/page.tsx"
      provides: "Income list page"
      contains: "Income"
    - path: "src/app/(app)/transactions/page.tsx"
      provides: "Transaction log page with filters"
      contains: "Transaction Log"
  key_links:
    - from: "src/app/(app)/expenses/page.tsx"
      to: "src/services/transaction.service.ts"
      via: "Server Action recordExpenseAction"
      pattern: "recordExpenseAction"
    - from: "src/app/(app)/transactions/page.tsx"
      to: "src/services/transaction.service.ts"
      via: "Effect.runPromise(listTransactions)"
      pattern: "listTransactions"
---

<objective>
Build the expense, income, and transaction log UI pages.

Purpose: FINC-02 (expenses), FINC-03 (income), and FINC-01 (transaction log) need user-facing pages. Expenses and income have their own dedicated pages with category management. The transaction log is a read-only filterable view of all debit/credit entries.
Output: Expenses page, income page, transaction log page with filtering.
</objective>

<execution_context>
@/Users/faridmatovu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/faridmatovu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-financial-reporting/04-CONTEXT.md
@.planning/phases/04-financial-reporting/04-UI-SPEC.md
@.planning/phases/04-financial-reporting/04-03-SUMMARY.md

<interfaces>
<!-- Transaction service exports (from Plan 03) -->
export const recordExpense: (input: CreateExpenseInput, actorId: string) => Effect
export const recordIncome: (input: CreateIncomeInput, actorId: string) => Effect
export const listTransactions: (filters: TransactionLogFilters, page: number, pageSize: number) => Effect
export const deleteTransaction: (id: string, actorId: string) => Effect

<!-- Category service exports (from Plan 03) -->
export const listCategories: (type?: "expense" | "income") => Effect
export const createCategory: (input: CreateCategoryInput, actorId: string) => Effect
export const deleteCategory: (id: string, actorId: string) => Effect

<!-- Server Actions (from Plan 03) -->
recordExpenseAction(formData), deleteExpenseAction(id), createExpenseCategoryAction(formData), deleteExpenseCategoryAction(id)
// income equivalents

<!-- UI-SPEC Interaction Contracts -->
Expense/income: Sheet from right edge, fields: Date, Category (select), Amount, Notes (optional)
"Add Category" inline link beneath category select — opens popover for new category name
Filters apply on change (debounced 300ms), "Clear filters" text button when any filter active
Transaction log filters: Date range (From/To), Type [All/Income/Expense], Category select
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expenses and income pages with category management</name>
  <files>
    src/app/(app)/expenses/page.tsx
    src/app/(app)/expenses/ExpenseListClient.tsx
    src/app/(app)/income/page.tsx
    src/app/(app)/income/IncomeListClient.tsx
  </files>
  <read_first>
    - /Users/faridmatovu/projects/money-lending/src/app/(app)/customers/page.tsx
    - /Users/faridmatovu/projects/money-lending/src/app/(app)/expenses/actions.ts
    - /Users/faridmatovu/projects/money-lending/src/app/(app)/income/actions.ts
    - /Users/faridmatovu/projects/money-lending/src/services/transaction.service.ts
    - /Users/faridmatovu/projects/money-lending/src/services/category.service.ts
  </read_first>
  <action>
**1. Create `src/app/(app)/expenses/page.tsx`** — Server Component:
- Page title: "Expenses", Primary CTA: "Add Expense" button (opens sheet)
- Fetch expense transactions: `Effect.runPromise(listTransactions({ type: "debit" }, 1, 50))`
- Fetch expense categories: `Effect.runPromise(listCategories("expense"))`
- Pass data to `ExpenseListClient`
- Empty state heading: "No expenses recorded", body: "Record your first expense to start tracking outflows."

**2. Create `src/app/(app)/expenses/ExpenseListClient.tsx`** — "use client":
- Table with columns: Date (DD/MM/YYYY), Category, Amount (UGX formatted), Notes, Actions (Delete button)
- "Add Expense" button at top right opens Sheet (shadcn Sheet from right edge)
- Sheet form fields:
  - Date: date input (type="date")
  - Category: select dropdown populated from expense categories
  - "Add Category" link below select — opens Popover with text input + "Add" button, calls `createExpenseCategoryAction`
  - Amount: number input with "UGX" prefix text
  - Notes: optional textarea
- Submit calls `recordExpenseAction` Server Action
- Delete button on each row triggers confirmation dialog:
  - Title: "Delete expense?"
  - Body: "This expense entry will be permanently removed. This cannot be undone."
  - Confirm: "Delete expense" (destructive variant), Cancel: "Keep expense"
  - Calls `deleteExpenseAction`
- Categories with transactions: delete button in category management is disabled, tooltip "Category has transactions and cannot be deleted"

**3. Create `src/app/(app)/income/page.tsx`** — Server Component:
- Same pattern as expenses but for income
- Page title: "Income", Primary CTA: "Add Income"
- Fetch income transactions: `Effect.runPromise(listTransactions({ type: "credit" }, 1, 50))` — but filter to exclude auto-posted entries OR show all including auto-posted (user decision: show all for transparency)
- Fetch income categories: `Effect.runPromise(listCategories("income"))`
- Empty state heading: "No income recorded", body: "Record your first income entry to start tracking inflows."

**4. Create `src/app/(app)/income/IncomeListClient.tsx`** — "use client":
- Same structure as ExpenseListClient but using income actions
- Delete confirmation: title "Delete income entry?", body "This income entry will be permanently removed. This cannot be undone.", confirm "Delete entry", cancel "Keep entry"
- Sheet fields identical pattern: Date, Category, Amount, Notes
  </action>
  <verify>
    <automated>cd /Users/faridmatovu/projects/money-lending && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(app)/expenses/page.tsx contains "Expenses" and "No expenses recorded"
    - src/app/(app)/expenses/ExpenseListClient.tsx contains `"use client"` and `recordExpenseAction`
    - src/app/(app)/expenses/ExpenseListClient.tsx contains "Delete expense?" (confirmation dialog)
    - src/app/(app)/expenses/ExpenseListClient.tsx contains "Add Category" or "createExpenseCategoryAction"
    - src/app/(app)/income/page.tsx contains "Income" and "No income recorded"
    - src/app/(app)/income/IncomeListClient.tsx contains `"use client"` and `recordIncomeAction`
    - src/app/(app)/income/IncomeListClient.tsx contains "Delete income entry?"
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Expenses and income pages display entries in tables, support adding/deleting entries via sheet and dialogs, and allow category management via inline popover</done>
</task>

<task type="auto">
  <name>Task 2: Transaction log page with filtering</name>
  <files>
    src/app/(app)/transactions/page.tsx
    src/app/(app)/transactions/TransactionLogClient.tsx
  </files>
  <read_first>
    - /Users/faridmatovu/projects/money-lending/src/app/(app)/customers/page.tsx
    - /Users/faridmatovu/projects/money-lending/src/services/transaction.service.ts
    - /Users/faridmatovu/projects/money-lending/src/services/category.service.ts
  </read_first>
  <action>
**1. Create `src/app/(app)/transactions/page.tsx`** — Server Component:
- Page title: "Transaction Log"
- Accept searchParams for filters: `type`, `categoryId`, `dateFrom`, `dateTo`, `page`
- Fetch data: `Effect.runPromise(listTransactions(filters, page, 50))`
- Fetch all categories (both types): `Effect.runPromise(listCategories())`
- Pass data + categories to TransactionLogClient
- Empty state heading: "No transactions yet", body: "Transactions appear here automatically when payments and creditor repayments are recorded."
- Export buttons: "Export PDF" and "Export Excel" — link to `/api/reports/transactions?format=pdf` and `?format=excel` (Route Handlers created in Plan 07)

**2. Create `src/app/(app)/transactions/TransactionLogClient.tsx`** — "use client":
- Filter bar at top (horizontal row):
  - Type filter: Select with options ["All", "Income", "Expense"] — maps to "credit"/"debit"/undefined
  - Category filter: Select populated from all categories
  - Date range: two date inputs (From / To)
  - "Clear filters" text button — visible only when any filter is active
- Filters apply on change with 300ms debounce via `useRouter().push()` with updated searchParams (Server Component data fetching pattern)
- Table columns: Date (DD/MM/YYYY), Type (badge — "Income" green / "Expense" red using `text-green-600` and `text-destructive`), Category, Description, Amount (UGX), Recorded By
- Pagination at bottom if total > pageSize
- No delete functionality on transaction log (entries managed via their source pages)
  </action>
  <verify>
    <automated>cd /Users/faridmatovu/projects/money-lending && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(app)/transactions/page.tsx contains "Transaction Log"
    - src/app/(app)/transactions/page.tsx contains `listTransactions` call
    - src/app/(app)/transactions/page.tsx contains "No transactions yet"
    - src/app/(app)/transactions/TransactionLogClient.tsx contains `"use client"`
    - src/app/(app)/transactions/TransactionLogClient.tsx contains filter for type (Income/Expense/All)
    - src/app/(app)/transactions/TransactionLogClient.tsx contains `text-green-600` (income color)
    - src/app/(app)/transactions/TransactionLogClient.tsx contains `text-destructive` (expense color)
    - src/app/(app)/transactions/TransactionLogClient.tsx contains debounce or setTimeout for 300
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Transaction log displays all entries with filtering by type, category, and date range — debounced filter application using URL searchParams</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `pnpm test` passes
- All three pages exist and compile: /expenses, /income, /transactions
</verification>

<success_criteria>
Users can record expenses and income with configurable categories, view the full transaction log with filters, and manage categories (add new, cannot delete if referenced). All financial entries flow through the unified transaction table.
</success_criteria>

<output>
After completion, create `.planning/phases/04-financial-reporting/04-05-SUMMARY.md`
</output>
